# 声呐三维重建 术语详解手册

> 面向入门理解：每个术语给出「含义 → 为什么重要/拓展 → 关联术语」，帮助建立从声呐物理到三维重建优化的完整概念体系。
> 配套 `声呐论文知识库.md` 使用。版本：2026-07-31

---

## 目录

1. [声呐硬件与物理原理](#一声呐硬件与物理原理)
2. [声呐成像几何](#二声呐成像几何)
3. [坐标系与位姿](#三坐标系与位姿)
4. [结构光运动恢复 / SLAM / BA](#四结构光运动恢复--slam--ba)
5. [非线性优化与数学工具](#五非线性优化与数学工具)
6. [特征提取与数据关联](#六特征提取与数据关联)
7. [鲁棒估计](#七鲁棒估计)
8. [点云与曲面重建](#八点云与曲面重建)
9. [深度学习与图像分割](#九深度学习与图像分割)
10. [评价指标](#十评价指标)
11. [传感器与平台](#十一传感器与平台)
12. [具名方法与专有名词](#十二具名方法与专有名词)

---

## 一、声呐硬件与物理原理

### 声呐（SONAR）
**含义**：Sound Navigation and Ranging，利用声波在水中传播来探测、定位、成像的技术。分**主动声呐**（自己发声、收回波）与**被动声呐**（只听目标发声）。成像声呐是主动式。
**拓展**：水中声速约 1500 m/s（随温度/盐度/压力变化），远低于光速，这决定了声呐"成像慢"——扫一个体积要时间。声波在水中衰减远小于光，故作用距离远（几十米到千米级）。
**关联**：主动声呐、声速、飞行时间。

### 前视声呐 / 成像声呐 / 声学相机（FLS, Forward-Looking Sonar / Imaging Sonar / Acoustic Camera）
**含义**：向前方锥形区域发射声波、接收回波强度并成二维图像的声呐。三个名字常混用。
**拓展**：它是本课题的核心传感器。输出的是"方位角×距离"的强度图，像一张扇形/极坐标灰度图。典型型号 DIDSON、Oculus、BlueView。
**关联**：视锥 frustum、方位角、距离、俯仰角模糊。

### 换能器 / 换能器阵列（Transducer / Transducer Array）
**含义**：把电信号↔声信号相互转换的元件。成像声呐用**一维线阵**（一排换能器）。
**拓展**：一维阵列是"只能在一个方向（方位）分辨来向"的根源——它能靠阵元间的相位差分辨方位角，但在垂直方向（俯仰）没有分辨能力，这直接导致俯仰角丢失。
**关联**：波束形成、方位角、俯仰角模糊。

### 波束形成（Beamforming）
**含义**：通过对阵列各阵元接收信号加权/延时求和，"聚焦"到特定方向，从而分辨回波来自哪个方位角的信号处理技术。
**拓展**：这是声呐"测方位角"的核心手段。方位分辨率取决于阵列孔径和频率。成像声呐方位精度通常优于 1°。
**关联**：换能器阵列、方位角、旁瓣。

### 飞行时间（ToF, Time of Flight）
**含义**：声波从发射到接收回波经历的时间。距离 = 声速 × 时间 / 2。
**拓展**：这是声呐"测距离"的核心手段，非常准。距离分辨率取决于脉冲带宽。
**关联**：距离、声速。

### 脉冲 / ping（Acoustic Pulse / Ping）
**含义**：声呐一次发射的声波脉冲。一个 ping 对应一帧或一次测量。
**拓展**：帧率受最大量程限制——量程越远，等回波回来越久，帧率越低（声速慢的代价）。
**关联**：帧率、量程。

### 相干斑噪声 / 散斑噪声（Speckle Noise）
**含义**：相干声波在粗糙表面反射后相互干涉，产生的颗粒状、乘性噪声。
**拓展**：这是声呐图像最主要、最难处理的噪声，呈**乘性**（与信号强度相关），不是普通高斯加性噪声，所以普通线性滤波（均值/高斯）效果差。故用 CFAR、非线性扩散（A-KAZE）、小波、专门去噪网络。
**关联**：CFAR、A-KAZE、多径、混响。

### 多径效应 / 多径鬼影（Multipath）
**含义**：声波经多次反射（如经水面、海底二次反弹）后才返回，在错误的更远距离处贡献虚假强度，形成"鬼影"。
**拓展**：造成图像里出现并不存在的"假目标"，污染特征与重建。神经渲染类方法（NeuSIS）会显式建模多径。
**关联**：混响、伪匹配、漂浮物伪影。

### 旁瓣（Sidelobe）
**含义**：波束形成时除主方向（主瓣）外，在其它方向也有较弱的响应，这些次级响应即旁瓣。
**拓展**：强反射体的旁瓣会在图像里"拖出"虚假亮纹/亮点，产生"类特征"杂波，几何上难与真实目标区分——正是创新点二想用分割去除的对象之一。
**关联**：波束形成、混响、杂波。

### 混响（Reverberation）
**含义**：海底、水体、水面等背景散射体产生的持续杂乱回波背景。
**拓展**：与目标回波混在一起，降低信噪比，产生背景杂波点。分海底混响、体积混响、水面混响。
**关联**：信噪比、杂波、目标-背景分割。

### 声学阴影 / 阴影区（Acoustic Shadow / Shadow Zone）
**含义**：目标遮挡声波，使其后方区域收不到回波，在图像上呈现的低强度暗区。
**拓展**：**极其重要**——阴影不是"没东西"，而是被前方物体挡住了。而且**阴影的形状/长度直接编码了目标的高度信息**：物体越高，投下的阴影越长。这就是创新点二"阴影→高度反演"的物理基础，用来补偿声呐丢失的俯仰角。
**关联**：俯仰角模糊、高度反演、掠射角、空间雕刻。

### 掠射角 / 掠入射角（Grazing Angle）
**含义**：声波入射方向与目标所在（海底）平面之间的夹角。
**拓展**：阴影长度换算目标高度的几何关系里，掠射角是关键参数（h ≈ 阴影长度 × tan(掠射角) 的量级关系，具体依几何而定）。掠射角小（近水平照射）时阴影拉得长、对高度更敏感。
**关联**：声学阴影、高度反演。

### 信噪比（SNR, Signal-to-Noise Ratio）
**含义**：有用信号功率与噪声功率之比。声呐图像 SNR 低。
**拓展**：低 SNR 是声呐一切困难的根源——特征易被噪声淹没、匹配易错。CFAR、鲁棒核、置信度加权都是应对低 SNR 的手段。
**关联**：相干斑、混响、鲁棒估计。

---

## 二、声呐成像几何

### 方位角 / 水平角（Bearing / Azimuth，常记 ψ 或 θ）
**含义**：目标相对声呐前向轴在水平面内的偏角。**可由波束形成测得**。
**拓展**：对应声呐图像的"列"（横向）。注意不同论文符号不统一：Huang 用 ψ，部分论文用 θ 表示方位。
**关联**：波束形成、俯仰角、距离。

### 距离 / 斜距（Range / Slant Range，常记 r 或 ρ）
**含义**：目标到声呐的直线距离，**由飞行时间测得**。
**拓展**：对应声呐图像的"行"（纵向）。"斜距"强调它是三维直线距离而非水平投影距离。
**关联**：飞行时间、量程、盲程。

### 俯仰角 / 仰角（Elevation，常记 θ 或 φ）
**含义**：目标相对声呐前向轴在竖直方向的偏角。**成像声呐无法测量它——这是核心难题**。
**拓展**：一维阵列在竖直方向无分辨力，俯仰视场内所有回波被压到同一像素。恢复它 = 声呐三维重建的本质任务。
**关联**：俯仰角模糊、俯仰弧、可观测性。

### 俯仰角模糊 / 仰角歧义（Elevation Ambiguity）
**含义**：因俯仰角丢失，一个图像像素对应的真实 3D 点位置不唯一。
**拓展**：这是声呐区别于光学相机的根本点。光学相机丢"距离"、声呐丢"俯仰角"，二者恰好互补。解法：多视几何（ASfM）、阴影先验、物理约束。
**关联**：俯仰弧、可观测性、退化运动。

### 俯仰弧 / 仰角弧（Elevation Arc）
**含义**：给定一个像素 (方位, 距离)，所有可能的真实 3D 点构成的一段圆弧（俯仰角在视场内变化）。
**拓展**：三维重建就是要在这段弧上确定点到底在哪。多帧观测的俯仰弧交集越小、约束越强；退化运动下弧几乎重合、无法定位。
**关联**：俯仰角模糊、三角化、退化运动。

### 视锥 / 视场（Frustum / Field of View, FOV）
**含义**：声呐能探测的锥形空间区域，由方位视场、俯仰视场、最小/最大量程界定。
**拓展**：视场是"视场硬约束"的物理来源——重建出的点必须落在视锥内，否则不合理。这是创新点一"视场箱式约束"的依据。
**关联**：方位视场、俯仰视场、量程、盲程。

### 方位视场 / 俯仰视场（Bearing FOV / Elevation FOV/Aperture）
**含义**：声呐在方位、俯仰方向的张角。DIDSON 典型方位 28.8°、俯仰 28°（加扩束镜）。
**拓展**：俯仰视场大 → 成像体积大、重叠多，但俯仰模糊严重（成像模式）；俯仰视场小（1°，剖面模式）→ 可近似俯仰=0，但重叠少。
**关联**：成像模式、剖面模式、扩束镜。

### 量程 / 盲程（Max/Min Range / Blind Zone）
**含义**：最大量程 = 能探测的最远距离；最小量程/盲程 = 太近的区域收不到有效回波。
**拓展**：盲程是视场约束的一部分。量程决定帧率上限。
**关联**：视场、帧率。

### 极坐标图像 vs 笛卡尔图像（Polar Image vs Cartesian Image）
**含义**：原始声呐数据是极坐标图（横轴方位角、纵轴距离）；显示/处理常转成笛卡尔图（扇形展开成真实空间比例）。
**拓展**：两者之间是相似变换（缩放+旋转+平移）。DISO 等算法在笛卡尔图上算梯度/关键点。像素↔(方位,距离)的映射标定是工程关键（本项目用线性自标定 beam=A·θ+B）。
**关联**：像素标定、相似变换。

### 扩束镜 / 剖面模式 / 成像模式（Spreader Lens / Profiling Mode / Imaging Mode）
**含义**：扩束镜是加装在声呐上扩大俯仰视场的镜头。剖面模式=窄俯仰视场（≈1°），成像模式=宽俯仰视场（≈28°）。
**拓展**：本课题面向成像模式（宽俯仰、强模糊），这才需要 ASfM/阴影等手段；剖面模式下俯仰≈0 问题简单但重叠少。
**关联**：俯仰视场、俯仰角模糊。

---

## 三、坐标系与位姿

### 位姿（Pose）
**含义**：物体在空间中的位置（3 维平移）+ 姿态（3 维旋转），合称 6 自由度（6-DoF）位姿。
**拓展**：声呐每帧有一个位姿。BA 要同时优化所有帧位姿。用 4×4 齐次变换矩阵 T 或 (x,y,z,roll,pitch,yaw) 六维向量表示。
**关联**：自由度、SE(3)、里程计。

### 自由度（DoF, Degrees of Freedom）
**含义**：描述系统状态所需的独立参数个数。刚体运动 6-DoF（3 平移+3 旋转）。
**拓展**：声呐单帧观测只给 2 维（方位+距离），却要恢复点的 3-DoF 位置——少一维就是俯仰模糊的量化说法。
**关联**：位姿、可观测性、可解性条件。

### 欧拉角 / roll-pitch-yaw（Euler Angles）
**含义**：用绕三个轴依次旋转（滚转 roll、俯仰 pitch、偏航 yaw）表示姿态。
**拓展**：直观但有**万向锁（gimbal lock）** 奇异问题。代码里 `euler_to_matrix` 用 R=Rz·Ry·Rx 约定。优化中大旋转建议用旋转向量/四元数避免奇异。
**关联**：旋转矩阵、四元数、万向锁。

### 旋转矩阵 / 旋转向量 / 四元数（Rotation Matrix / Rotation Vector / Quaternion）
**含义**：表示 3D 旋转的三种方式。旋转矩阵 3×3 正交；旋转向量=轴×角（3 维，最小参数化）；四元数 4 维无奇异。
**拓展**：优化中旋转是"过参数化"的（矩阵 9 个数只有 3 自由度），故用**指数映射**在流形上做最小参数化更新。代码 `rot_to_vec` 就是矩阵→旋转向量。
**关联**：指数映射、SO(3)、李代数。

### SE(3) / SO(3) / 李群李代数（Lie Group / Lie Algebra）
**含义**：SO(3)=所有 3D 旋转构成的群；SE(3)=旋转+平移的刚体变换群。其李代数 so(3)/se(3) 是"切空间"，可做加减。
**拓展**：位姿优化在流形上进行——用指数映射 exp 把李代数（旋转向量/6 维增量）映到李群（位姿），保证更新后仍是合法旋转。Lin 2025 用 (δω,δt)∈se(3) 优化位姿即此。
**关联**：指数映射、流形优化、旋转向量。

### 指数映射 / 对数映射（Exponential / Logarithmic Map）
**含义**：exp 把李代数元素（如旋转向量）映射为李群元素（旋转矩阵/位姿）；log 反之。
**拓展**：优化每步算出的是李代数里的增量 Δ，用 exp 施加到当前位姿：T ← T·exp(Δ)。这是"流形上的加法"。
**关联**：SE(3)、旋转向量、流形优化。

### 里程计 / 航位推算（Odometry / Dead Reckoning）
**含义**：靠自身传感器（IMU、DVL）积分推算相对运动/轨迹，无需外部参考。
**拓展**：提供 BA 的**位姿初值**和相邻帧软约束，但会**累积漂移**（越走越偏）。BA 的作用之一就是用多视观测反向修正里程计漂移。
**关联**：IMU、DVL、漂移、预积分。

### 漂移（Drift）
**含义**：里程计误差随时间/距离累积，导致轨迹逐渐偏离真值。
**拓展**：x、y、偏航靠积分 → 有漂移；z、俯仰、滚转有重力/深度绝对参考 → 无漂移（Lin 2025 的漂移建模）。回环检测可消漂移，但纯里程计不能。
**关联**：里程计、回环检测、位姿图。

### 规范自由度 / gauge（Gauge Freedom）
**含义**：整个重建可以整体平移/旋转而不改变任何观测残差，这种全局不确定性即规范自由度。
**拓展**：必须固定（如固定第 0 帧位姿 = 加"先验因子"）才能让优化有唯一解，否则解可漂移。代码里 `w_prior` 强权重固定首帧即此。
**关联**：先验因子、可解性。

---

## 四、结构光运动恢复 / SLAM / BA

### 结构光运动恢复（SfM, Structure from Motion）
**含义**：从多张不同视角图像中，同时恢复场景三维结构（点）与相机运动（位姿）的技术。
**拓展**：光学 SfM 成熟（如 COLMAP）。核心是"多视几何约束"。声呐版叫 ASfM。
**关联**：ASfM、BA、三角化。

### 声学结构光运动恢复（ASfM, Acoustic Structure from Motion）
**含义**：把 SfM 迁移到声呐——从多帧声呐图像恢复稀疏 3D 点 + 声呐位姿，用多视约束解俯仰模糊。
**拓展**：Huang & Kaess 2015 奠基。与光学 SfM 的关键差异：投影模型不同（丢俯仰而非丢距离）、存在退化运动、需处理欠约束路标。
**关联**：BA、因子图、退化运动。

### 光束平差 / 束调整（BA, Bundle Adjustment）
**含义**：SfM 的核心优化步骤——联合调整所有位姿与所有 3D 点，使"重投影误差"最小。名字源于光学里"光束"汇聚。
**拓展**：本质是大规模非线性最小二乘。声呐 BA 的残差是 (方位,距离) 或像素误差。你的 `ba_optimize.py` 就是声呐 BA。
**关联**：重投影误差、因子图、非线性最小二乘。

### 同时定位与建图（SLAM, Simultaneous Localization and Mapping）
**含义**：机器人在未知环境中一边估计自身位姿、一边建地图。
**拓展**：SfM 偏离线批处理、SLAM 偏在线增量。声呐 SLAM 可用 ASfM 作后端。含前端（特征/关联）+ 后端（优化）+ 回环。
**关联**：ASfM、位姿图、回环检测。

### 因子图（Factor Graph）
**含义**：一种二部图，用"变量节点"（位姿、路标）和"因子节点"（观测、里程计、先验）表示优化问题的结构。
**拓展**：SLAM/BA 的标准建模工具（GTSAM、iSAM 库）。每个因子=一个残差项。边连接因子与它约束的变量。稀疏结构使求解高效。
**关联**：变量节点、因子、GTSAM、iSAM。

### 路标 / 地标（Landmark）
**含义**：环境中被重复观测、用于定位与建图的三维特征点。
**拓展**：BA 优化的对象之一。声呐路标可用"世界笛卡尔坐标"或"相对基准帧的球坐标"参数化，后者更线性（Westman）。
**关联**：特征点、参数化、基准帧。

### 基准帧 / 锚定帧（Base Pose / Anchor Frame）
**含义**：某路标"首次被观测"的那一帧，用作该路标球坐标参数化的参考。
**拓展**：相对球坐标参数化里，路标 = 相对基准帧的 (方位,距离,俯仰)，比全局笛卡尔更线性、优化更稳。你的 `12.5新.py` 用此法。
**关联**：相对球坐标参数化、路标。

### 重投影误差（Reprojection Error）
**含义**：把当前估计的 3D 点用当前估计位姿投影回图像，与实际观测像素之间的差。
**拓展**：BA 要最小化的核心量。声呐里是预测 (方位,距离) 与观测之差（或转成像素）。你的报告里 RMS 5.31→1.02 px 就是它。
**关联**：BA、残差、像素量纲。

### 反投影（Backprojection）
**含义**：由一个像素观测 (方位,距离) + 假设俯仰角，反推出一个 3D 点。
**拓展**：路标初始化常用——设俯仰=0 反投影得初值。因俯仰未知，反投影不唯一（落在俯仰弧上）。
**关联**：三角化、俯仰弧、初始化。

### 三角化（Triangulation）
**含义**：用同一点在多帧的观测，交会求出其 3D 位置。
**拓展**：声呐三角化 = 多帧俯仰弧求交。弧交集小则准，退化运动下弧重合则失败。是点云生成的基础。
**关联**：俯仰弧、多视几何、可解性条件。

### 可解性条件（Existence of Solution）
**含义**：系统是否有足够约束求出唯一解的判据。Huang 给出 6(n−1)+3m ≤ 2mn（n 帧 m 点）。
**拓展**：例如 3 帧至少需 4 点。约束不足则欠定。
**关联**：自由度、可观测性、欠约束路标。

### 退化运动（Degenerate Motion）
**含义**：某些运动方式下俯仰角无法恢复（俯仰弧重合或对称）。
**拓展**：Huang 2015 分析：纯 X 平移、纯偏航+Y 平移是退化的；一般运动、滚转、俯仰+Z 良约束。设计采集轨迹要避开退化运动。
**关联**：俯仰弧、可观测性、可解性。

### 可观测性（Observability）
**含义**：系统状态能否由观测唯一确定的性质。俯仰角在声呐里是"弱可观测"维。
**拓展**：可用信息矩阵/$A^TA$ 的最小特征值量化——特征值趋 0 = 不可观测/病态。改进版思想路线建议用 Fisher 信息量化病态。
**关联**：Fisher 信息、退化运动、病态。

### 欠约束 / 良约束路标（Under/Well-constrained Landmark）
**含义**：观测能否充分约束路标俯仰角。纯偏航下俯仰弧重合 → 欠约束；纯滚转 → 良约束。
**拓展**：Westman 2018 用 $A^TA$ 特征值比 λ₂/λ₃<ρ 判定，欠约束路标不硬三角化（会污染位姿），改用非参数/半参数建模。
**关联**：可观测性、特征值判据。

### 位姿图（Pose Graph）
**含义**：只优化位姿（节点）间相对约束（边）的简化图优化，不含路标。
**拓展**：比全 BA 轻量，常用于回环后的全局校正。
**关联**：因子图、回环检测、BA。

### 回环检测（Loop Closure）
**含义**：识别机器人回到曾经到过的地方，加一条约束消除累积漂移。
**拓展**：DISO、SIO 等里程计方法都缺回环（论文明说是局限）。有回环才能全局一致。
**关联**：漂移、位姿图、SLAM。

---

## 五、非线性优化与数学工具

### 非线性最小二乘（Nonlinear Least Squares, NLS）
**含义**：最小化残差平方和 Σ‖r(x)‖² 的问题，r 关于 x 非线性。BA 就是 NLS。
**拓展**：通过反复"线性化→解线性系统→更新"迭代求解（高斯牛顿/LM）。
**关联**：高斯牛顿、LM、雅可比。

### 雅可比矩阵（Jacobian）
**含义**：残差对优化变量的一阶偏导矩阵 J=∂r/∂x。
**拓展**：线性化的核心。可数值差分（慢、通用）或解析推导（快、准，SIO/DISO 都给了解析式）。大规模问题必须用解析雅可比 + 稀疏结构。
**关联**：线性化、高斯牛顿、稀疏性。

### 高斯牛顿 / 列文伯格-马夸尔特（Gauss-Newton / Levenberg-Marquardt, LM）
**含义**：两种解 NLS 的迭代法。GN 快但可能发散；LM 在 GN 与梯度下降间自适应，更稳健。
**拓展**：LM 加"阻尼因子"，病态时更保守。Huang/Westman 用 LM。退化运动下 LM 迭代次数剧增（收敛慢）是病态信号。
**关联**：NLS、信赖域、阻尼。

### 信赖域反射（TRF, Trust-Region Reflective）
**含义**：scipy `least_squares` 的一种方法，支持**变量边界约束**的信赖域算法。
**拓展**：你要做"视场箱式约束"必须用 `trf`（`lm` 不支持 bounds）。`ba_optimize.py` 用 trf+huber。
**关联**：信赖域、箱式约束、bounds。

### 正规方程 / Cholesky 分解（Normal Equations / Cholesky）
**含义**：线性化后解 AᵀAΔ=Aᵀb 即正规方程；Cholesky 把 AᵀA 分解为 RᵀR 高效求解。
**拓展**：大规模 BA 的 AᵀA 是稀疏的，稀疏 Cholesky 是高效求解关键（你的 C# 版 ba.cs 用此）。
**关联**：雅可比、稀疏性、iSAM。

### 马氏距离（Mahalanobis Distance）
**含义**：考虑协方差的距离 ‖x‖²_Σ = xᵀΣ⁻¹x，即用不确定度加权的距离。
**拓展**：残差按其测量不确定度加权（协方差白化），使不同量纲/精度的观测公平比较。声呐里方位精度高、俯仰精度低应体现在协方差里。
**关联**：协方差、信息矩阵、白化。

### 协方差 / 信息矩阵 / 白化（Covariance / Information Matrix / Whitening）
**含义**：协方差 Σ 描述测量不确定度；信息矩阵 = Σ⁻¹；白化 = 用 Σ^(−1/2) 把残差归一化为标准正态。
**拓展**：把 Σ^(−1/2) 吸收进雅可比 A 的行，是 BA 里加权的标准做法。你报告里"按 σ_θ、σ_ρ 白化"就是让方位/距离残差按各自精度加权。
**关联**：马氏距离、各向异性加权。

### 先验因子（Prior Factor）
**含义**：对某变量施加的先验约束（如固定第 0 帧位姿）。
**拓展**：用于消规范自由度、稳定欠约束变量（如路标弱先验拉向初值）。权重体现先验强度。
**关联**：规范自由度、路标弱先验。

### 箱式约束 / 带边界优化（Box Constraint / Bounded Optimization）
**含义**：把变量限制在区间内（下界 ≤ x ≤ 上界）的优化。
**拓展**：创新点一"视场硬约束"= 把路标俯仰角限制在 [θmin,θmax]。须用支持 bounds 的求解器（TRF）。注意：约束限制解范围，但**不增加可观测性**。
**关联**：TRF、视场、可观测性。

---

## 六、特征提取与数据关联

### 特征点 / 关键点（Feature Point / Keypoint）
**含义**：图像中显著、可重复检测、可跨帧匹配的点（角点、高梯度点等）。
**拓展**：稀疏重建的基本单元。声呐低 SNR 下检测难，常用 A-KAZE、高梯度点。
**关联**：描述子、数据关联。

### 描述子（Descriptor）
**含义**：刻画特征点局部外观的向量，用于匹配（比较相似度）。
**拓展**：光学用 SIFT/ORB。声呐图像太"糊"，描述子区分度低，故有的方法（Westman）干脆只用几何而不用描述子匹配。
**关联**：特征点、数据关联、A-KAZE。

### A-KAZE
**含义**：一种基于**非线性尺度空间**（各向异性扩散）的特征检测/描述算法。
**拓展**：关键优点——非线性扩散平滑噪声的同时保留高梯度边界，特别适合高散斑的声呐图像，是声呐特征法的常用选择。
**关联**：特征点、相干斑、非线性扩散。

### 数据关联（Data Association）
**含义**：确定不同帧的哪些观测对应同一个 3D 路标。
**拓展**：SLAM 前端核心，也是最易出错处。错关联严重污染 BA。声呐因图像非直观、点序会变，关联尤其难。
**关联**：门控、χ² 检验、软关联。

### 门控（Gating）
**含义**：用一个阈值/区域预筛候选匹配，超出门限的直接排除，缩小搜索空间。
**拓展**：Huang 2016 用重投影落在 (0.2m,4°) 门限内作候选。是数据关联的第一步剪枝。
**关联**：数据关联、χ² 检验。

### χ² 检验（Chi-square Test）
**含义**：用卡方分布判断一组残差是否在统计上合理（是否该接受某关联假设）。
**拓展**：Huang 2016 用 χ²_{d,α}（α=0.99）阈值接受数据关联假设，惩罚"空匹配"。也用于外点剔除。
**关联**：门控、数据关联、外点。

### 联合相容分支定界（JCBB, Joint Compatibility Branch and Bound）
**含义**：一种考虑"所有匹配联合相容性"（而非逐个）的数据关联算法，用树搜索+剪枝找最优假设。
**拓展**：比最近邻关联更抗错。Huang 2016 的对应树思路类似 JCBB。
**关联**：数据关联、χ² 检验。

### 软数据关联 / 可切换约束（Soft Data Association / Switchable Constraints）
**含义**：不做非黑即白的硬关联，而给每个匹配一个连续可信度权重，在优化中动态调整。
**拓展**：创新点一模块一。**关键陷阱**：权重自由优化会被全压到 0（退化），故须像 Switchable Constraints（Sünderhauf）那样对权重加惩罚项，或用 Dynamic Covariance Scaling（DCS）。
**关联**：鲁棒核、退化、置信度流。

### 外点 / 内点（Outlier / Inlier）
**含义**：外点=错误/异常观测（误匹配、粗差）；内点=正确观测。
**拓展**：鲁棒估计的目标是让外点不主导优化。RANSAC、Huber、χ² 都是识别/抑制外点的手段。
**关联**：鲁棒核、RANSAC、χ² 检验。

### RANSAC（随机采样一致性）
**含义**：反复随机取最小样本拟合模型、统计内点，选内点最多的模型，从而排除外点。
**拓展**：硬剔除的代表。**缺点**：极低 SNR 下会误删真特征、留隐蔽伪匹配——这正是创新点一改用"软关联"的动机。
**关联**：外点、软关联、鲁棒估计。

---

## 七、鲁棒估计

### 鲁棒核 / M 估计（Robust Kernel / M-estimator）
**含义**：用非二次的损失函数降低大残差（外点）的影响，使优化对粗差鲁棒。
**拓展**：普通最小二乘对外点极敏感（平方放大）。鲁棒核在残差大时"压平"惩罚。常见：Huber、Cauchy、Geman-McClure、Tukey。
**关联**：Huber、IRLS、外点。

### Huber 核
**含义**：残差小于阈值 δ 时用二次（保持精度），大于 δ 时转线性（抑制外点）的鲁棒损失。
**拓展**：温和、常用、凸。`ba_optimize.py` 用 δ=20px。δ 要有物理意义（故残差用像素量纲）。
**关联**：鲁棒核、IRLS、f_scale。

### 迭代重加权最小二乘（IRLS, Iteratively Reweighted Least Squares）
**含义**：把鲁棒核问题转化为"每次迭代按当前残差给每个观测重新算权重、再解加权最小二乘"的方法。
**拓展**：鲁棒核的标准求解方式。残差大的观测权重自动变小。
**关联**：鲁棒核、Huber、权重。

### 渐进非凸 / 图优化非凸调度（GNC, Graduated Non-Convexity）
**含义**：先用凸/宽松的损失求全局大致解，再逐步"收紧"成非凸鲁棒损失，避免陷入局部极小。
**拓展**：Yang & Carlone 2020 的 GNC 是当前鲁棒 SLAM 热点。改进版思想路线的"鲁棒核阈值渐进调度"即此思想。
**关联**：鲁棒核、局部极小、非凸优化。

### 动态协方差缩放（DCS, Dynamic Covariance Scaling）
**含义**：根据残差动态缩放约束的协方差（等价动态降权），一种闭式的鲁棒方法。
**拓展**：与 Switchable Constraints 相关但更高效。可作软关联权重的理论依据之一。
**关联**：软关联、可切换约束、鲁棒核。

---

## 八、点云与曲面重建

### 点云（Point Cloud）
**含义**：三维点的集合，每点含 XYZ（可含强度/法向）。重建的中间产物。
**拓展**：声呐点云稀疏、非均匀、含漂浮物噪点。存为 .ply/.pcd，用 MeshLab/CloudCompare 查看。
**关联**：三角化、曲面重建、法向。

### 法向量估计（Normal Estimation）
**含义**：估计每个点所在局部表面的朝向（法线方向）。
**拓展**：曲面重建（泊松）的必需输入。稀疏噪声点云法向估计易错。常用局部邻域 PCA。创新点一模块四用"加权协方差"让法向只信赖高置信点。
**关联**：曲面重建、泊松、PCA。

### 泊松曲面重建 / 屏蔽泊松（Poisson / Screened Poisson Surface Reconstruction）
**含义**：把"由带法向点云重建曲面"表述为求解泊松方程，得到光滑水密网格。屏蔽泊松（Kazhdan 2013）加数据项更贴合点。
**拓展**：经典稠密成面方法。创新点一模块四=**加权**泊松（按置信度加权）。目前你的代码里还没有成面步骤，需补原型。
**关联**：法向、网格、Marching Cubes。

### 网格 / 三角网格（Mesh / Triangle Mesh）
**含义**：由顶点+三角面片构成的曲面表示。重建的最终产物。
**拓展**：由点云经泊松/Marching Cubes 生成。评价用 Chamfer/Hausdorff 距离与真值网格比。
**关联**：点云、泊松、Marching Cubes。

### Marching Cubes（移动立方体）
**含义**：从隐式场（如 SDF、占据栅格）提取等值面（如零水平集）生成三角网格的经典算法。
**拓展**：神经隐式方法（NeuSIS）用它从 SDF 提表面。
**关联**：SDF、隐式表面、网格。

### 有符号距离函数 / 隐式表面（SDF / Implicit Surface）
**含义**：SDF(x)=点 x 到最近表面的有符号距离（内负外正），零水平集即表面。
**拓展**：神经隐式重建用神经网络表示 SDF。是"隐式"表面（不直接存点/面，而是存一个函数）。
**关联**：Marching Cubes、神经隐式、NeuSIS。

### 空间雕刻（Space Carving）
**含义**：用每帧"最近高强度回波之前是自由空间"的信息，从一个初始实体里"雕掉"自由空间，剩下的即物体。
**拓展**：声呐稠密重建经典法。缺点：需大量视角、无法重建凹面。Fermat 路径法是其推广。
**关联**：占据栅格、Fermat 路径、稠密重建。

### 稀疏重建 vs 稠密重建（Sparse vs Dense Reconstruction）
**含义**：稀疏=只重建离散特征点（ASfM/BA）；稠密=重建连续表面/密集点（泊松、神经隐式、空间雕刻）。
**拓展**：你的路线是"稀疏 BA → 点云 → 加权泊松稠密成面"。神经隐式是稠密环节的替代范式（详见知识库 §11.5）。
**关联**：ASfM、泊松、神经隐式。

---

## 九、深度学习与图像分割

### 图像分割 / 语义分割 / 实例分割（Image / Semantic / Instance Segmentation）
**含义**：把图像逐像素分类。语义分割=按类别（目标/背景/阴影）；实例分割=区分同类的不同个体。
**拓展**：创新点二用它分出目标区域、声学阴影区域。为几何重建提供区域级先验。
**关联**：掩码、SAM、mIoU。

### 掩码（Mask）
**含义**：标记某区域的二值/多值图（1=属于该类，0=否）。
**拓展**：分割输出即掩码。目标掩码用于只在目标区提特征、过滤点云杂波。
**关联**：分割、目标-背景分割。

### SAM（Segment Anything Model）
**含义**：Meta 提出的可提示、通用图像分割基础模型（foundation model），零样本泛化强。
**拓展**：创新点二主方法基座。直接用于声呐有域间隙，需微调（LoRA）适配。
**关联**：基础模型、LoRA、域适应。

### 基础模型 / 参数高效微调 / LoRA（Foundation Model / PEFT / LoRA）
**含义**：基础模型=大规模预训练通用模型；PEFT=只调少量参数适配下游；LoRA=在权重旁加低秩矩阵、只训这些低秩层。
**拓展**：LoRA 冻结 SAM 主干、只训低秩适配层，用**少量声呐标注**完成域适应——适合声呐小样本。是创新点二"方法可辩护"的关键（小样本高效）。
**关联**：SAM、域适应、小样本。

### 域间隙 / 域适应 / sim-to-real（Domain Gap / Adaptation / Sim-to-Real）
**含义**：训练域与应用域分布不同（如自然图像 vs 声呐图像；仿真 vs 真实）导致性能下降；域适应=缩小该差距。
**拓展**：Aubard 综述强调声呐 DL 的 sim-to-real 是大挑战。你的阴影反演若只在仿真验证，须处理 sim-to-real 质疑。
**关联**：LoRA、数据增强、仿真器。

### YOLO-seg
**含义**：YOLO 系列的实例分割版本，轻量实时。
**拓展**：创新点二的对比基线（反衬 ViT+LoRA 在小样本/精度上的优势）。
**关联**：实例分割、基线。

### 数据增强（Data Augmentation）
**含义**：对训练数据做变换（旋转、加噪、几何扰动）扩充样本、提泛化。
**拓展**：声呐专用增强——相干斑仿真、扇形/极坐标几何增广、量程/波束扰动。ControlNet 类生成模型也可合成声呐图像补数据。
**关联**：小样本、sim-to-real、ControlNet。

---

## 十、评价指标

### 绝对轨迹误差 / 相对位姿误差（ATE / RPE）
**含义**：ATE=估计轨迹对齐真值后的全局平移误差（衡量全局一致性）；RPE=相邻帧相对位姿误差（衡量局部漂移）。
**拓展**：里程计/SLAM 标准指标。ATE 看整体、RPE 看逐帧。
**关联**：位姿、漂移、里程计。

### 重投影 RMS（Reprojection RMS）
**含义**：所有观测重投影误差的均方根。
**拓展**：BA 质量的直接指标。你的 5.31→1.02 px 即此，亚像素级说明位姿/点云一致性好。
**关联**：重投影误差、BA。

### 倒角距离 / 豪斯多夫距离（Chamfer Distance / Hausdorff Distance）
**含义**：衡量两个点云/网格差异。CD=双向最近点距离的平均；Hausdorff=最坏（最大）最近点距离。
**拓展**：三维重建精度的标准指标。CD 看平均质量、Hausdorff 看最大偏差（对离群点敏感）。Lin 2025 用 Hausdorff。
**关联**：点云、网格、重建精度。

### mIoU / Dice
**含义**：分割精度指标。IoU=交并比；mIoU=各类 IoU 平均；Dice=2×交/(和)，与 IoU 类似。
**拓展**：评价目标/阴影分割质量。阴影分割准确率直接影响高度反演精度。
**关联**：分割、掩码。

---

## 十一、传感器与平台

### DIDSON / Oculus / BlueView
**含义**：三款常见成像声呐。DIDSON（SoundMetrics，多篇论文用）、Oculus（Blueprint）、BlueView（Teledyne）。
**拓展**：DIDSON 典型：方位 28.8°、俯仰 28°（加扩束镜）、96 方位 bin×512 距离 bin。选型影响视场、分辨率、量程。
**关联**：视场、扩束镜、量程。

### IMU（惯性测量单元）
**含义**：含陀螺仪（测角速度）+ 加速度计（测加速度）的传感器，积分得姿态/速度/位置。
**拓展**：提供高频运动估计，但积分漂移。与 DVL 融合做水下里程计。
**关联**：里程计、漂移、预积分、DVL。

### DVL（多普勒测速仪, Doppler Velocity Log）
**含义**：靠多普勒效应测量载体相对海底/水层的三轴速度。
**拓展**：水下定位关键传感器，速度积分得位置。DVL+IMU（+深度计）融合是标准水下里程计配置。
**关联**：IMU、里程计、漂移。

### IMU 预积分（IMU Preintegration）
**含义**：把两关键帧之间的多个 IMU 测量预先积分成一个相对运动约束，避免优化每次迭代重复积分。
**拓展**：Forster 提出，视觉/声惯性里程计标配（SIO 用）。大幅降计算量。
**关联**：IMU、里程计、因子图。

### AUV / ROV / HAUV
**含义**：AUV=自主水下航行器（无缆自主）；ROV=遥控水下航行器（有缆遥控）；HAUV=悬停式 AUV（Bluefin，多篇论文实验平台）。
**拓展**：搭载声呐/IMU/DVL 采集数据。移动慢（如 0.3 m/s），有利于假设帧间小运动。
**关联**：里程计、数据采集。

### 扩展卡尔曼滤波（EKF, Extended Kalman Filter）
**含义**：处理非线性系统的递归状态估计滤波器。
**拓展**：DVL+IMU 融合出里程计常用 EKF（DISO 用）。滤波（只用当前+历史压缩）vs 平滑/BA（用全部历史）——后者更准但更重。
**关联**：里程计、平滑、BA。

---

## 十二、具名方法与专有名词

### ASfM 系列（Huang & Kaess）
**含义**：声呐 SfM 奠基工作（2015 建模、2016 数据关联）。
**拓展**：因子图 + LM，多视解俯仰模糊，退化运动分析。本课题的直接理论根基。
**关联**：BA、因子图、退化运动。

### 欠约束路标 SLAM（Westman 2018）
**含义**：A-KAZE 全自动 + 特征值判据识别良/欠约束路标 + 非/半参数建模。
**拓展**：相对球坐标参数化的来源。本课题 `12.5新.py` 用其思想。
**关联**：欠约束路标、相对球坐标。

### Fermat 路径（Westman 2020）
**含义**：用图像列强度不连续点 + 沿 z 轴平移估距离梯度，单发重建表面点（差分立体）。
**拓展**：不依赖强度值/反射率，是空间雕刻的推广，能重建凹面。
**关联**：空间雕刻、镜面/边界点。

### DISO（直接成像声呐里程计, 2024）
**含义**：不提特征，直接最小化高梯度点强度误差估帧间位姿的里程计系统。
**拓展**：前端强度、后端重投影；金字塔粗到精。假设零俯仰（限 2D 平面）。
**关联**：直接法、里程计、窗口优化。

### NeuSIS（神经隐式声呐重建, Qadri 2022）
**含义**：用神经隐式 SDF + 可微声学体渲染做稠密重建。
**拓展**：稠密环节的神经范式，需精确位姿。本课题**不实现**，仅对标定位（知识库 §11.5）。
**关联**：SDF、可微渲染、稠密重建。

### 可微渲染 / 体渲染（Differentiable / Volume Rendering）
**含义**：把"从三维表示生成图像"的过程写成可微函数，从而能用图像损失反向优化三维表示。
**拓展**：NeRF/NeuS/NeuSIS 的核心。声呐版建模声波沿俯仰弧/射线的能量积分。
**关联**：SDF、神经隐式、透射率。

### 恒虚警率（CFAR, Constant False Alarm Rate）
**含义**：从雷达借来的自适应阈值检测——根据像素周围背景噪声动态定阈值，保持恒定虚警概率。
**拓展**：声呐去噪/目标分割常用。Lin 的工作用 CFAR+DBSCAN 去噪。
**关联**：去噪、SNR、DBSCAN。

### DBSCAN
**含义**：基于密度的聚类算法，能发现任意形状簇并识别噪声点。
**拓展**：CFAR 检测后用 DBSCAN 聚类去孤立噪点。
**关联**：CFAR、去噪、聚类。

---

## 附：符号约定速查

| 符号 | 含义 | 备注 |
|---|---|---|
| ψ / θ | 方位角 bearing | 不同论文可能互换 |
| r / ρ | 距离/斜距 range | |
| θ / φ | 俯仰角 elevation | 不可测，核心难点 |
| R, t | 旋转矩阵、平移 | 位姿 |
| T | 4×4 齐次变换 | SE(3) |
| Σ | 协方差矩阵 | 加权用 Σ⁻¹ |
| λ₃ | AᵀA 最小特征值 | 量化俯仰可观测性 |
| h | 目标高度 | 阴影反演目标 |
| δ | Huber 阈值 | 鲁棒核 |
