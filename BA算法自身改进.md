# BA 算法自身改进（对照论文集）

> 范围：**只讨论 BA 算法本身的改进**，不含与上游的数据对接（对接见 `BA环节总结与上游对接.md`）。
> 依据：`ba_optimize.py` 现状 + `声呐论文知识库.md` 中论文结论。
> 版本：2026-07-31

---

## 0. 一句话结论

当前 `ba_optimize.py` 是"能收敛的基础版"。对照论文，最欠三块：
1. **没区分欠约束路标**（Westman 2018 核心）→ 俯仰方向乱跑、污染位姿；
2. **参数化没跟前端统一**（该用相对球坐标，Westman/Huang）→ 系统非线性、不稳；
3. **没有可观测性/退化防护**（Huang 2015 核心）→ 退化运动下发散或对称错解。

---

## 1. 路标参数化：世界笛卡尔 → 相对球坐标 ★★★

**现状**：`ba_optimize.py` 路标用世界系笛卡尔 (x,y,z)；而前端 `12.5新.py` 用相对基准帧球坐标 (ψ,r,θ)。**两份代码不一致**。

**问题**：Westman 2018、Huang 明确——声呐路标用相对基准帧球坐标参数化**更线性、系统更稳定**；笛卡尔下欠约束路标在 z 方向自由漂移，无法表达"俯仰在弧上不确定"这一结构。

**改进**：
- BA 路标改成相对"基准帧（首次观测帧）"的 (ψ, r, θ)，俯仰角 θ 作为显式变量；
- 优化后再转世界 XYZ 输出（与 `12.5新.py` 的 `optimize()` 末尾一致）；
- 好处：θ 可直接加范围约束/先验（见 §5）。

**依据**：Westman 2018 §III-B、Huang 2016。

---

## 2. 欠约束路标识别与分类建模 ★★★（最关键）

**现状**：所有路标一视同仁当 3-DOF 优化 + 统一弱先验（拉向初值）。

**问题**：Westman 2018 的核心结论——**强行三角化欠约束路标会污染位姿估计**。report 里"路标最大位移 2.25 m、集中在 Z 方向"正是欠约束点在俯仰方向乱跑，当前只靠弱先验压，不彻底。

**改进（Westman 三件套）**：
1. **判别**：对每个路标构建单点线性化最小二乘，算 $A^TA$（3×3）特征值 λ₁≥λ₂≥λ₃，用判据 **λ₂/λ₃ < ρ**（如 ρ=20）分良/欠约束；
2. **良约束**：正常 3-DOF 球坐标优化；
3. **欠约束**：两种建模任选
   - 非参数：把路标移出状态向量，因子内在 [θmin,θmax] **网格搜索**取最小重投影的 θ*（等价俯仰均匀分布）；
   - 半参数：只移除 θ，把 (ψ,r) 显式建模为正态。
4. **升级**：欠约束路标一旦满足判据可升级为良约束。

**依据**：Westman 2018（论文集已有）。

---

## 3. 求解效率：数值雅可比+稠密 → 解析雅可比+稀疏 ★★

**现状**：`scipy.least_squares(method="trf")` 用数值差分雅可比 + 稠密求解。

**问题**：10 帧+217 点能跑；一旦用满 14786 观测/每帧建位姿，数值差分极慢、稠密矩阵爆内存。

**改进**：
- 推导并实现**解析雅可比**（声呐重投影对位姿/路标的偏导，SIO/DISO 论文给了完整链式解析式，含 ∂(θ,ρ)/∂P_body · ∂P_body/∂位姿）；
- 利用 BA 稀疏结构：**Schur 补**边缘化路标 + **稀疏 Cholesky**（Huang 的 iSAM/GTSAM 思路，你的 C# 版 `ba.cs` 已是此方向）；
- 或直接迁移到 GTSAM/Ceres。

**依据**：Huang 2015（Cholesky/iSAM）、Dong SIO（解析 Jacobian）、DISO。

---

## 4. 鲁棒性：固定 Huber → GNC + χ² 外点剔除 ★★

**现状**：Huber δ=20px 固定；report 显示外点 0→0，说明 **δ 太松没触发**。

**问题**：错关联只靠松 Huber 兜不住；固定阈值易陷局部极小。

**改进**：
1. **渐进非凸 GNC**：先用宽松（近凸）损失求全局大致解，逐步收紧到非凸鲁棒核，避开局部极小；
2. 配合 **χ² 门限**做外点剔除迭代（Huang 2016，α=0.99）；
3. 长远引入**软数据关联/可切换约束**（对权重加惩罚防塌缩，思想路线创新一模块一）。

**依据**：Huang 2016（χ²）、GNC（Yang & Carlone 2020）、Switchable Constraints（Sünderhauf 2012）。

---

## 5. 可观测性量化 + 视场约束 ★★

**现状**：无可观测性分析；视场约束在代码里缺失（`12.5新.py` 的视场判断还是量纲错误的死代码）。

**问题**：Huang 2015 明确——**纯 X 平移、纯偏航+Y 平移退化**，俯仰不可恢复，BA 发散或给对称错解。

**改进**：
1. 用 Fisher 信息 / $A^TA$ 最小特征值 λ₃ **量化俯仰可观测性**（λ₃→0 即病态）；
2. 弱可观测方向加先验；
3. 实现**视场箱式硬约束**：把 θ 限在物理俯仰孔径 [θmin,θmax]，用 `trf`+bounds（注意：约束只限制解范围，**不增加可观测性**，真正补可观测靠运动多样性或阴影先验）。

**依据**：Huang 2015（退化运动）、Westman 2018（特征值判据）。

---

## 6. 利用声呐特有信息：阴影 / 强度 ★★

**现状**：只用点特征 (θ,ρ) 重投影。

**问题**：声呐图像的**声学阴影**（编码高度/俯仰）与**强度**信息完全没用，纯几何点信息量有限。

**改进**：
- **阴影→高度先验**注入 BA（Fermat 2020 / Aykin 思想，正是思想路线创新点二核心）——带不确定度作软观测；
- 或**强度直接法**约束（DISO：最小化高梯度点强度误差）。

**依据**：Westman 2020 Fermat、DISO、Aykin & Negahdaripour。

---

## 7. 初始化稳健性 ★

**现状**：直接吃上游 `landmarks_final` 当初值。

**问题**：纯几何 BA 对初值敏感，初值差易陷局部极小。

**改进**：BA 内置**俯仰=0 反投影 + 多视仰角弧线性三角化**做初值/兜底，不完全依赖上游初值质量。

**依据**：Huang 2015（反投影初始化）、Westman（线性三角化）。

---

## 8. 输出不确定度 ★

**现状**：只输出优化后位姿/点云，无协方差。

**改进**：由收敛时信息矩阵 $A^TA$ 求逆，输出**位姿/路标协方差**，用于精度评估、退化判断、下游加权（report 已列为待办）。

**依据**：通用 SLAM 实践、Westman（协方差判可观测性）。

---

## 9. 优先级汇总

| 优先 | 改进 | 论文来源 | 收益 |
|---|---|---|---|
| ★★★ | §2 欠约束路标识别+分类建模 | Westman 2018 | 止住 Z 方向乱跑、位姿更准 |
| ★★★ | §1 路标改相对球坐标（与前端统一） | Westman/Huang | 系统更线性稳定 |
| ★★ | §3 解析雅可比 + 稀疏求解 | Huang/SIO/DISO | 支撑用满观测、提速 |
| ★★ | §4 GNC + χ² 外点剔除 | GNC/Huang 2016 | 真正抗错关联 |
| ★★ | §5 可观测性量化 + 视场约束 | Huang 2015/Westman | 抗退化发散 |
| ★★ | §6 阴影/强度信息 | Fermat/DISO/Aykin | 补俯仰、对接创新点二 |
| ★ | §7 内置三角化初始化 | Huang 2015 | 抗差初值 |
| ★ | §8 输出协方差 | 通用 | 精度评估 |

---

## 10. 建议的实施顺序（小步快跑，不破坏现有能跑版本）

1. **先做 §1+§2**（参数化统一 + 欠约束处理）：改动集中在残差构建，收益最大，直接解决 report 里 Z 乱跑问题。
2. **再做 §5**（可观测性量化 + 视场约束）：加 λ₃ 诊断 + `trf` bounds，防退化。
3. **然后 §4**（GNC + χ²）：提鲁棒。
4. **§3**（解析雅可比+稀疏）：为"用满 14786 观测"铺路。
5. **§6/§7/§8**：与思想路线创新点二（阴影）合并推进，输出协方差收尾。

> 实施原则：每步保留旧逻辑作 fallback（如"检测到球坐标输入才启用欠约束分类"），确保任何一步都不把现在能跑的流程改坏。
