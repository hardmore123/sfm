# 项目交接文档（写给零上下文的新对话）

> 更新时间：2026-07-23。工作根目录：`c:\Users\likunyuan\Desktop\private document\sfm\`（下称“根目录”，路径含空格与中文）。

---

## 0. 一句话概览

本项目围绕**水下二维前视声呐三维重建**，同时推进三条线：
1. **BA 代码**：对上游给的初始位姿+点云+数据关联做光束平差（Bundle Adjustment）联合优化；
2. **发明专利**：把该方法写成专利（已到“定稿”版）；
3. **大论文思路**：规划两个创新点的整体技术路线。

---

## 1. 目录结构与关键文件

```
sfm/
├── 12.5新.py                      # 最初的原型代码（像素投影+相对球坐标，含 bug，仅作参考，勿直接用）
├── ba_optimize.py                 # ★当前主用的 BA 代码（Python 参考实现，逻辑正确）
├── ba.cs / ba.exe                 # ★C# 实现版（之前没真 Python 时用它跑出结果；与 ba_optimize.py 同逻辑）
├── poses_est.npy                  # 输入：初始关键帧位姿 (10,4,4)，body→world 变换矩阵
├── pose_frame_ids.npy             # 输入：关键帧编号 [0,15,20,25,30,35,40,45,50,55]
├── landmarks_final.npy            # 输入：初始点云 (217,3)，世界坐标 XYZ
├── landmarks_final.ply            # 输入点云的 PLY 版
├── tracks.csv                     # 输入：数据关联（每帧每条 track 的 theta/rho/beam/range 等）
├── output/BA_2026-07-23_110328/   # BA 运行产物（优化位姿/点云/PLY + 三维图/俯视图/残差直方图 + report.md + ba_log.txt）
├── 专利/
│   ├── 专利最新初稿.md            # 用户原始初稿
│   ├── 专利重构稿.md              # 我重构后的版本（结构对齐、修 bug）
│   ├── 专利定稿.md                # ★最新：符号统一、去英文/去AI痕迹、术语规范
│   ├── 专利格式要求(1).md         # 专利撰写格式要求（务必遵守）
│   ├── 02B...docx                 # 主参考模板（频域-空间域小样本声呐重建）
│   ├── 附图说明与画法.md          # 五张附图的画法说明
│   └── 专利旧稿.md                # 历史草稿（含大量可复用素材）
├── 专利图/
│   ├── 图1..图3 彩色版(.png)、图4.jpg、图5.jpg   # 旧的彩色/JPG 版（不合规，待替换）
│   ├── 图1_整体流程图_黑白.png 等 5 张          # ★我用 C# 生成的黑白合规草图
│   └── draw_patent_figs.cs / _build.ps1         # 出图程序（可重生成）
└── 大论文思想路线/
    ├── 大论文整体思路.md          # ★最新版整体思路（创新点二定为 ViT+LoRA 分割）
    ├── 大论文整体思路-0723.md     # 旧版备份
    ├── 创新点一_问题定义与模块分解.md
    ├── 创新点二_问题定义与模块分解.md
    └── 总体技术路线图.png         # C# 生成的路线图
```

---

## 2. 现在在做的任务

- **主线 A（代码）**：把 `ba_optimize.py` 在**真 Python** 下端到端跑通并验证（此前只有 C# 版能跑）。
- **主线 B（专利）**：`专利定稿.md` 已完成符号/表达完善；待用户确认是否把剩余英文缩写（BA/SfM/RANSAC/IMU/DVL）也全中文化。
- **主线 C（论文）**：思路文档已成体系；创新点二（图像分割引导）尚未写代码。

---

## 3. 已经完成的

**代码 / BA**
- 读懂数据：`poses_est`(body→world 位姿)、`landmarks_final`(世界 XYZ 点云)、`tracks.csv`(数据关联)。
- 恢复了 `track_id → 点云行号` 映射（重投影匹配，成功关联 201/217）。
- 声呐观测用**像素量纲**：自标定 `beam≈249.87·theta+127.5`、`range≈277.04·rho`。
- BA 残差项：首帧先验 + 里程计相对位姿(SE3) + 声呐重投影(像素) + 路标弱先验；**Huber 鲁棒核 δ=20**。
- 里程计当作“真实但不完美”的软约束联合优化（用户确认里程计是真实数据）。
- **实测关键结果：重投影 RMS 由 5.307 px 降到 1.016 px（↓约 81%）**；方位角 RMS 0.01986→0.00384 rad，斜距 RMS 0.00625→0.00271 m。
- 产物已归档到 `output/BA_2026-07-23_110328/`（含三维图、俯视图、残差直方图、report.md）。
- `ba_optimize.py` 的 matplotlib 后端已改为 `Agg`（无弹窗、不阻塞、可保存图片）。

**专利**
- `专利定稿.md`：完整结构（摘要/技术领域/背景/发明内容[无公式]/有益效果/附图说明/具体实施方式[含公式与符号]/实验表）。
- 修正了原稿 4 处技术错误：投影与初始化互逆(几何一致)、“预积分”改“航位递推”、Huber 与 EM 双重鲁棒化的矛盾（统一为 Huber-IRLS 半二次）、视场约束与位姿纠偏的因果表述。
- 加入 4 个优选创新点（多视图仰角弧初始化、各向异性信息矩阵、俯仰角可观测性自适应、鲁棒核渐进调度）。
- 符号统一：$P^b/P^w$、$\hat r/\hat\phi$、$E_p$、$\Lambda_o$、$\theta_v$、$\mathcal I_0$；去英文单词、去 AI 痕迹。
- 五张附图已生成**黑白合规草图**（`专利图/*_黑白.png`）。

**论文思路**
- `大论文整体思路.md`：创新点一=几何优化层 BA；创新点二=ViT+LoRA 图像分割引导（核心落点“声学阴影→高度/仰角先验”）；两者“图像理解↔几何优化”双层闭环。

---

## 4. 当前状态 / 卡在哪里

- **已解决的历史卡点**：本机之前没有真 Python（只有 Store 占位桩），所以一直用 C# 版跑。**现在已安装 Python 3.14**，并已 `pip install` 好 `numpy 2.5.1 / scipy 1.18.0 / matplotlib 3.11.1`（cp314 轮子）。
- **当前待做的第一件事**：用真 Python **实际运行 `ba_optimize.py` 验证**（此前只在 C# 验证过）。命令见第 6 节。尚未确认 Python 版跑通（依赖刚装完）。

---

## 5. 下一步计划（按优先级）

1. **跑通并验证 `ba_optimize.py`**（真 Python），核对输出与 C# 版一致（重投影 RMS≈1.02px）。
2. **让代码对齐专利核心创新**（目前代码与专利有差距，见第 7 节“坑 6”）：
   - 地标参数化改为**相对球坐标(锚定帧) + 视场箱式硬约束 Ω**（用 scipy least_squares 的 bounds / TRF）；
   - 加**软关联置信度 $w^{(0)}$**（描述子距离负指数核）× Huber 权重；
   - 补 **S4 曲面重建**（置信度加权协方差法向 + 加权泊松）；
   - 补 4 个优选模块。
3. **专利**：确认是否全中文化缩写；补真实的“无视场约束”对比数值（表1第二行现为示例值 2.834）。
4. **论文创新点二**：实现声呐图像 目标/背景/**声学阴影** 分割（ViT+LoRA），把阴影高度先验注入 BA。

---

## 6. 如何运行（真 Python）

真解释器路径（**用这个，别用 WindowsApps 的**）：
```
C:\Users\likunyuan\AppData\Local\Programs\Python\Python314\python.exe
```
运行 BA：
```
cd "c:\Users\likunyuan\Desktop\private document\sfm"
python ba_optimize.py
```
输出：`poses_optimized.npy`、`landmarks_optimized.npy`、`landmarks_optimized.ply`、`ba_result.png`、`ba_topview.png`（写在脚本所在目录）。

重生成专利黑白附图（C#，无需 Python）：
```
powershell -ExecutionPolicy Bypass -File "c:\Users\likunyuan\Desktop\private document\sfm\专利图\_build.ps1"
```

---

## 7. 踩过的坑 —— 绝对不要再踩

1. **`python` 曾是 Microsoft Store 占位桩**（`...\WindowsApps\python.exe`，0 字节）。非交互调用会**挂起、无输出、无报错**。现已装真 Python 3.14 且排在 PATH 前面；跑代码认准 `...\Programs\Python\Python314\python.exe`。可 `where python` 确认。

2. **终端工具(execute_pwsh)有假象**：命令回显会**逐字符重复**、且几乎总是返回 **exit code -1**，但**命令其实已执行**（文件会被正确创建）。判断成功与否要**看产物文件**，不要看 exit code。

3. **PowerShell 5.1 用 GBK 读取 .ps1 文件**：脚本里写**中文字面量会乱码**导致语法错误。规避：**脚本内只写 ASCII**；用 `$PSScriptRoot` 代替中文路径字面量；`Set-Content -Encoding ascii`；且 **PS5.1 不支持三元运算符 `?:`**。

4. **中文路径/文件名**：.NET（C#）读写中文文件名**没问题**（C# 用 `AppDomain.CurrentDomain.BaseDirectory` 定位即可）；但**PowerShell 脚本内的中文字面量**、以及把中文路径硬编码进 `.ps1` 会乱码。运行 `.ps1` 时用 `-File "完整中文路径"` 传参是可以的（外壳能处理），坏的是**脚本文件内部**的中文。

5. **数据语义坑**：
   - `landmarks_final.npy` 的**行号 ≠ track_id**（它是被三角化的 track 子集按序排列），必须用重投影匹配恢复映射，别假设 index==track_id。
   - `poses_est` 是 **body→world**：`P_body = R^T (P_world − t)`。
   - 声呐观测：`theta`(方位角)、`rho`(斜距=3D 距离)；重投影用 `theta=atan2(y,x)`、`rho=‖P_body‖`；再线性映射到像素 beam/range。
   - Huber **δ=20 是像素量纲**（作用在 beam/range 残差上），别放到 rad/m 的原始残差上（那样 δ=20 永不触发）。

6. **代码 ≠ 专利（重要认知）**：当前 `ba_optimize.py`/`ba.cs` 用的是**世界系 XYZ 参数化 + 路标弱先验**，**没有**专利里宣称的“相对球坐标 + 视场箱式硬约束 Ω”这个**核心创新**；软关联 $w^{(0)}$、S4 曲面重建、4 个优选、创新点二分割也**都没在代码里**。写论文/答辩前必须把这些补进代码，否则“文档领先于代码”。

7. **12.5新.py 有 bug**：它对已是弧度的 `arctan2` 结果又乘了一次 `np.radians`，且用像素投影+相对球坐标参数化。**别直接复用**，以 `ba_optimize.py` 为准。

8. **matplotlib**：已用 `Agg` 后端。若改回交互后端(TkAgg)在无显示器/后台会**阻塞或报错**。批处理保持 `Agg`。

---

## 8. 关键数字备查
- 关键帧 10 个（帧号 0,15,20,25,30,35,40,45,50,55）；点云 217；track 关联成功 201。
- BA 参与观测约 1651；重投影 RMS 5.307→1.016 px。
- 像素标定：`beam≈249.87·theta+127.5`，`range≈277.04·rho−3.0`。
- Huber δ=20；里程计平移/旋转权重各 100；首帧先验权重 1000。
