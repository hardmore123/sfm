# 声呐三维重建与运动估计 知识库

> 基于 `论文集/` 中论文整理。聚焦成像声呐（前视声呐 FLS）的结构光运动恢复（ASfM）、束调整（BA）、里程计与三维重建。
> 整理日期：2026-07-31

---

## 目录

1. [基础：成像声呐几何与俯仰角模糊](#1-基础成像声呐几何与俯仰角模糊)
2. [声学结构光运动恢复 ASfM（Huang & Kaess）](#2-声学结构光运动恢复-asfmhuang--kaess)
3. [ASfM 数据关联（Huang & Kaess 2016）](#3-asfm-数据关联huang--kaess-2016)
4. [欠约束路标的特征 SLAM（Westman 2018）](#4-欠约束路标的特征-slamwestman-2018)
5. [声呐特征检测与匹配（Lin, CFAR + A-KAZE）](#5-声呐特征检测与匹配lin-cfar--a-kaze)
6. [Fermat 路径三维重建（Westman 2020）](#6-fermat-路径三维重建westman-2020)
7. [直接法声呐里程计 DISO（Xu 2024）](#7-直接法声呐里程计-disoxu-2024)
8. [声惯性里程计 SIO（Dong 2022）](#8-声惯性里程计-siodong-2022)
9. [多波束声呐三维运动估计（Huang 2026，声学 PnP）](#9-多波束声呐三维运动估计huang-2026声学-pnp)
10. [神经隐式声呐重建 NeuSIS（Qadri 2022）](#10-神经隐式声呐重建-neusisqadri-2022)
11. [位姿漂移下的神经三维重建（Lin 2025）](#11-位姿漂移下的神经三维重建lin-2025)
    - [11.5 关键辨析：稀疏经典稠密 vs 神经隐式稠密](#115-关键辨析稀疏经典稠密-vs-神经隐式稠密两种范式关系与定位)
12. [FLS 深度学习 6D 位姿估计（Yu 2026）](#12-fls-深度学习-6d-位姿估计yu-2026)
13. [声呐深度学习综述（Aubard 2025）](#13-声呐深度学习综述aubard-2025)
14. [测深 SfM：两介质折射校正（Dietrich 2017）](#14-测深-sfm两介质折射校正dietrich-2017)
15. [方法对比总表](#15-方法对比总表)
16. [关键公式速查](#16-关键公式速查)
17. [术语表](#17-术语表)
18. [声呐物理原理详解](#18-声呐物理原理详解)
19. [声呐图像坐标与像素映射细节](#19-声呐图像坐标与像素映射细节)
20. [各论文实现细节与设计思想](#20-各论文实现细节与设计思想)
21. [工程实现要点与调参经验](#21-工程实现要点与调参经验)

---

## 1. 基础：成像声呐几何与俯仰角模糊

**核心矛盾**：成像声呐（前视声呐 FLS）测量目标的**方位角 ψ（bearing/azimuth）** 和 **距离 r（range）**，但**丢失俯仰角 θ（elevation）**。这与单目光学相机恰好互补——相机测方位角与俯仰角但丢失深度（距离）。

- 声呐向一个锥形空间（frustum，视锥）发射声脉冲，用一维换能器阵列（transducer array）接收回波。
- 距离 r 由声波飞行时间（time of flight）和水中声速求得。
- 方位角 ψ 由阵列不同接收器接收同一信号的时间差求得，精度通常 < 1°。
- **俯仰角模糊**：同一条"俯仰弧（elevation arc）"上的所有 3D 点都投影到图像同一像素。低像素强度并不代表无障碍（还有阴影 shadow zone）。

**球坐标 ↔ 笛卡尔坐标转换**（多篇论文统一使用）：

$$
C=\begin{bmatrix}x\\y\\z\end{bmatrix}=r\begin{bmatrix}\cos\psi\cos\theta\\\sin\psi\cos\theta\\\sin\theta\end{bmatrix},\quad
S=\begin{bmatrix}\psi\\r\\\theta\end{bmatrix}=\begin{bmatrix}\arctan2(y,x)\\\sqrt{x^2+y^2+z^2}\\\arctan2(z,\sqrt{x^2+y^2})\end{bmatrix}
$$

**测量模型（丢俯仰角）**：像素只给出 (ψ, r)：
$$ r=\sqrt{x_s^2+y_s^2+z_s^2},\quad \psi=\arctan2(y_s,x_s) $$

**反投影初始化**：观测首帧时设 θ=0 做反投影得到 3D 初值：
$$ [x_s,y_s,z_s]^T=r[\cos\psi\cos\theta,\ \sin\psi\cos\theta,\ \sin\theta]^T,\ \theta=0 $$

**典型传感器参数（SoundMetrics DIDSON 300m，多篇复用）**：
- 方位视场 ψmax = 28.8°，俯仰视场（加扩束镜 spreader lens）28°
- "剖面模式（profiling mode）"俯仰视场可小至 1°，可近似 θ≈0；"成像模式（imaging mode）"俯仰视场大（28°），重叠体积大但模糊严重
- 96 个方位 bin，512 个距离 bin；范围常用 0.375–9.375 m（或 0.75–5.25 m）
- 其它常见传感器：Oculus M 系列（俯仰约 20°）、BlueView P900

---

## 2. 声学结构光运动恢复 ASfM（Huang & Kaess）

**论文**：*Towards Acoustic Structure from Motion for Imaging Sonar*, IROS 2015（`Huang和Kaess - 2015`，与 `Towards_acoustic_structure_from_motion` 为同一篇）。ASfM 的奠基工作。

**核心思想**：把光学 SfM/束调整（bundle adjustment）思想迁移到声呐——用**多个（>2）声呐视角**观测同一场景，同时恢复稀疏点特征的 3D 位置与声呐位姿，**不做任何平面假设**。相比只用两帧的成对（pairwise）方法，多视角提供更多约束、精度更高。

**因子图（factor graph）建模**：
- 变量节点：位姿 xᵢ（6 DOF）、点路标 lⱼ（3 DOF）
- 因子节点：里程计 uᵢ、特征观测 mₖ、首位姿先验 p（定义参考系，消除不可约束自由度）
- MAP 目标（高斯噪声下化为非线性最小二乘）：

$$
\Theta^*=\arg\min_x\Big[\|x_0\|^2_\Lambda+\sum_{k=1}^m\|h(x_{i_k},l_{j_k})-m_k\|^2_{\Xi_k}+\sum_{i=1}^n\|g(x_{i-1},x_i)-u_i\|^2_{\Lambda_i}\Big]
$$

- 用 Mahalanobis 距离 $\|x\|^2_\Sigma=x^T\Sigma^{-1}x$；求解用 Gauss-Newton / Levenberg-Marquardt（LM）迭代线性化，Cholesky 分解求正规方程 $A^TA\Delta=A^Tb$。旋转用指数映射（exponential map）局部最小参数化。

**可解性条件**（n 个位姿、m 个点，全可见）：
$$ 6(n-1)+3m\le 2mn $$
例：3 个视角至少需要 4 个点才能完全约束。

**退化运动（degenerate cases）——重点**：以下运动无法解出俯仰角：
| 运动类型 | 是否退化 | 说明 |
|---|---|---|
| 一般运动（general） | 否，最佳 | 平均误差最小(~0.11m)，2次LM收敛 |
| 俯仰+Z平移（pitch+Z） | 否，良约束 | 俯仰弧交集小 |
| 纯 X 平移（forward） | **是** | 弧在两处相交，产生关于零平面对称的模糊 |
| 纯偏航+Y平移（yaw+Y） | **是** | 弧大面积重叠，z 难恢复 |
| 纯滚转（roll） | 否，良约束 | 弧绕真实俯仰点旋转 |

退化案例中 LM 迭代次数剧增（纯X≈74次、yaw+Y≈95次），代价函数近似平坦。**结论**：需要能改变俯仰弧几何的运动（滚转、俯仰+Z）才能约束好；额外传感器（IMU）可帮助。

**实验**：仿真 Monte Carlo（1000 次）+ 真实 DIDSON 数据（船坞梯子，手动选5帧特征）。

---

## 3. ASfM 数据关联（Huang & Kaess 2016）

**论文**：*Incremental Data Association for Acoustic Structure from Motion*, IROS 2016。补足 ASfM 流水线中"自动找对应"的一环。

**难点**：声呐图像非直观——3D 结构在图像中的顺序与真实不符；声呐移动/旋转会改变特征点在图像中的前后顺序（因为到声呐距离变了），连人工关联都困难。

**增量数据关联算法**：
1. **门控（gating）剪枝**：对每个新特征，把历史路标反投影/重投影到当前帧，若重投影落在门限内（经验值 0.2 m 距离、4°方位）则作候选。仅观测过一次的路标沿俯仰弧按 1° 间隔采样后重投影。
2. **对应树（correspondence tree）**：类似联合相容分支定界（Joint Compatibility Branch and Bound, JCBB）。每层是一个特征观测及其候选匹配（含"空匹配 null match"=新路标）。
3. **假设选择**：按 χ² 检验（自由度 d、置信 α=0.99）接受，惩罚空匹配数——选满足 χ²、空匹配最少、优化残差最小的假设。
4. 若两测量彼此过近（<1° 方位或 <0.2 m 距离）则**丢弃避免歧义**；测试假设时用**批量（batch）建图**而非增量，避免陷入局部极小。

**性能**：小噪声无杂散特征准确率 90.9%，大噪声 78.9%；含杂散降到 81.6% / 72.4%。C++ 实现平均 14–16 ms。复杂度 O(m·lᵐ·log l)。

---

## 4. 欠约束路标的特征 SLAM（Westman 2018）

**论文**：*Feature-based SLAM for Imaging Sonar with Under-constrained Landmarks*, ICRA 2018。把 ASfM 变成全自动、并妥善处理"欠约束路标"。

**三大贡献**：
1. **全自动流水线**：A-KAZE 特征提取 + 基于几何的**投影数据关联**（Algorithm 1，用阈值 D1、D2 保守匹配，不用描述子相似度）。
2. **良约束路标检测**：对单个路标做线性化最小二乘 $l_j^*=\arg\min\sum\|h_i(l_j)-z_i\|^2_{\Sigma_i}$，检查 $A^TA$（3×3）的特征值。俯仰角完全不可约束时秩亏。判据：$\lambda_2/\lambda_3<\rho$（ρ 可调，如20）才算良约束。也可用退化因子/条件数等判据。
3. **欠约束路标的两种建模**（关键创新，避免退化污染位姿）：
   - **非参数表示（Method 1）**：把欠约束路标从状态向量移除，用一个组合因子 fⱼ，每次迭代在可行俯仰范围内**均匀采样搜索**最优 θ*（最小重投影误差），等价于把俯仰角信念当作均匀分布。
   - **半参数表示（Method 2）**：只移除俯仰角，把路标显式建模为 2D 方位-距离点（正态分布），可被优化更新。

**关键洞见**：把欠约束路标（如纯偏航导致俯仰弧完全重合）**朴素地**塞进标准 ASfM 会让优化秩亏、位姿估计变差。分层处理（良约束 3-DOF 球坐标；欠约束用上述两法），可上升（upgrade）已变良约束的路标。

**参数化选择**：相对"基准位姿（base pose）"的球坐标参数化比全局笛卡尔更线性、更适合。用 GTSAM + LM。真实数据 ATE 达厘米级。

---

## 5. 声呐特征检测与匹配（Lin, CFAR + A-KAZE）

**论文**：*Sonar Image Feature Detection and Matching for Acoustic Structure from Motion*（Tianxiang Lin, CMU）。为特征法 ASfM 做前处理。

**两大技术**：
1. **CFAR（恒虚警率，Constant False Alarm Rate）去噪/分割**：从雷达图像处理借鉴。逐像素自适应阈值：
   $$ t=\alpha\big(\textstyle\sum_{N}I-\sum_{G}I\big)/n,\quad \alpha=n(P_{fa}^{-1/n}-1) $$
   其中 N 参考单元、G 保护单元、n 测试单元像素数、P_fa 虚警概率。之后用 **DBSCAN** 聚类去噪。PSNR 优于小波变换（25.66 vs 23.76 vs 原始 20.09）。
2. **结构-空间损失 SSL（Structural-and-Spatial Loss）去除误匹配**：
   - 结构项：对匹配点邻域 (2r+1) 方块算 **SSIM**（结构相似度），归一化。
   - 空间项：两个空间向量 v=(μ_x, μ_y, μ_r)（特征点所在列/行/方块平均强度）的归一化欧氏距离。

**实验**：HoloOcean 仿真 + DIDSON 真实数据；A-KAZE 在 CFAR 掩膜内检测特征。局限：缺真实位姿，无定量精度分析。

---

## 6. Fermat 路径三维重建（Westman 2020）

**论文**：*A Theory of Fermat Paths for 3D Imaging Sonar Reconstruction*, IROS 2020。不依赖强度值/反射率，用"差分立体"重建表面点。

**核心**：把 3D 声呐重建与**非视距（NLOS）重建**理论联系起来。对每个方位角 θᵢ 定义一个 2D 平面 πᵢ（含 z 轴），在平面内推导 **2D Fermat 流方程（Fermat flow equation）**。

**Fermat 集**：距离函数 r(x;c) 取局部极值或鞍点的表面点，分两类：
- **边界集 B(c)**：法向未定义的点（每条连续曲面在平面内最多2个），可跨视角对应、多观测非线性优化重建。
- **镜面集 S(c)**：产生镜面反射的点（无穷多，不可跨视角对应），但可"单发（single-shot）"重建。

**Fermat 流方程**（可单帧重建）：
$$ x_F=c-r_F\nabla_c r_F(c),\qquad \nabla_c r_F(c)=-\frac{x_F-c}{\|x_F-c\|}\ (\text{单位向量}) $$

**求解**：沿声呐 **z 轴平移**估计距离梯度 ∂r_F/∂z（拟合二次多项式 $r_F(z)=a_2z^2+a_1z+a_0$ 抗噪，等价于加曲率/平滑先验），因梯度是单位向量，w 轴分量可推得。Fermat 路径在图像列中表现为**强度不连续（discontinuity）**，用 Canny 边缘检测。

**与空间雕刻（space carving）关系**：本法是空间雕刻的推广。凸面两者等价；**凹面**空间雕刻无法重建（只得外壳），Fermat 法能重建。真实实验平面靶平均误差 1.3 cm。

---

## 7. 直接法声呐里程计 DISO（Xu 2024）

**论文**：*DISO: Direct Imaging Sonar Odometry*, ICRA 2024（`DISO` 与 `Xu 等 - 2024` 同一篇）。开源：github.com/SenseRoboticsLab/DISO。

**核心**：**直接法（direct method）**——不提特征描述子、不做特征匹配，直接**最小化高强度梯度点的声学强度误差**估计帧间相对变换。动机：短时间内路标强度（受反射系数、法向、距离、遮挡影响）相对稳定，可作约束；比逐像素匹配更抗噪。

**强度残差**（假设零俯仰角，只保留 x,y 分量）：
$$ E_I=\sum_{I_p\in K}\|I_c(^{I_c}p_i)-I_r(^{I_r}p_i)\|^2 $$

**系统（类视觉直接法 SLAM）**：
- 预处理：极坐标→笛卡尔图像；Sobel 算子算强度梯度；网格滤波取高梯度关键点（top N，网格内取最大保证分布均匀）。
- 惯性里程计：DVL + IMU 经 EKF 融合，30Hz 提供初值。
- 前端：帧到帧跟踪（多尺度金字塔，粗到精）→ 帧到窗口跟踪（抗漂移，剔除强度误差过大的外点）。
- 后端：**窗口优化**改用**重投影误差**（不用强度误差，因窗口时间长、强度波动大且免去重采样图像）：
  $$ E_P=\sum\|S_{IC}\Pi(T^{-1}_{S_0S_j}\,{}^{S_0}l_i)-{}^{I_j}p_i\|^2 $$
  加里程计旋转/平移残差 E_OR、E_OP。因子图联合优化路标与位姿。

**性能**：仿真（DAVE）+ 真实（Aracati2017）均优于 ICP 基线 BlueROV SLAM（对初值不敏感、抗外点）。**局限**：无回环；零俯仰假设限于 2D 平面场景。

---

## 8. 声惯性里程计 SIO（Dong 2022）

**论文**：*Bundle Adjustment-Based Sonar-Inertial Odometry for Underwater Navigation*, ROBIO 2022（西北工业大学）。

**核心**：紧耦合 FLS + IMU 的束调整里程计，**无环境先验假设**。因子图两类变量（状态 xᵢ、特征 lⱼ）、两类因子：
1. **IMU 预积分因子（preintegration）**：只算声呐帧间相对约束，降低重复积分开销。残差 e_imu 15×1（位置/姿态/速度/加计零偏/陀螺零偏），用中点积分。
2. **声呐重投影误差因子**：借鉴视觉重投影。用 η=1/cosθ 表达投影关系 $[p_c;0]=K P_c$。残差 $e_{sjk}=h(x_j,P_{wk})-\hat p_{sjk}=[r_k-\hat r_k,\ \psi_k-\hat\psi_k]^T$。给出完整解析 Jacobian（对两帧位姿的旋转/平移增量求导）。

初值：IMU 航位推算（dead reckoning）提供帧间运动、首帧反投影得特征初值。基于 Ceres。仿真优于纯 IMU 航位推算；X 轴、偏航约束较弱。**局限**：无回环，误差随轨迹累积。

---

## 9. 多波束声呐三维运动估计（Huang 2026，声学 PnP）

**论文**：*Three-Dimensional Motion Estimation of Multibeam Imaging Sonar*, CIAC 2025 / LNEE 1503（中科院自动化所，`978-981-95-4049-5`）。

**核心**：完整声呐里程计流水线，**无平面假设、无人工声标**：
1. **两视图声学束调整（two-view acoustic BA）** 初始化（2D-2D）
2. **3D-2D 声学 PnP（Perspective-n-Point）** 求解
3. **全局束调整（global BA）** 优化，充分利用历史观测

**关键贡献**：
- 用**基于图的声学 SfM** 克服声呐非线性投影模型导致的"闭式解失效"。
- **运动方向先验（motion direction prior）**：解决俯仰角不确定引起的**对称双位姿模糊（symmetric dual-pose ambiguity）**（声呐对极几何 epipolar geometry 中的固有问题）。

在自研仿真环境（2D 平面场景 + 随机 3D 环境）验证。

---

## 10. 神经隐式声呐重建 NeuSIS（Qadri 2022）

**论文**：*Neural Implicit Surface Reconstruction using Imaging Sonar*, `2209.08221`（CMU）。开源：github.com/rpl-cmu/neusis。

**核心**：首个用于宽俯仰视场声呐的**基于物理的可微体渲染器（differentiable volumetric renderer）**做稠密三维重建。三要素：
1. 可微体渲染器，建模声学球面波前传播。
2. 用神经隐式函数的**零水平集（SDF, signed distance function）** 表示 3D 表面。
3. 正则化的渲染损失。

**图像形成模型**（像素强度=沿俯仰弧、距离窗内反射声能积分）：
$$ I_p=\int_{\phi_{min}}^{\phi_{max}}\int_{r_i-\epsilon}^{r_i+\epsilon}\frac{E_e}{r}T(r,\theta_i,\phi)\sigma(r,\theta_i,\phi)\,dr\,d\phi $$
T 为透射率（transmittance），σ 为粒子密度。离散化后沿声学弧和声学射线采样近似。

优势：相比点云/体素网格，隐式表示无内存开销、精度更高，且**无监督**（不需大标注集）。

---

## 11. 位姿漂移下的神经三维重建（Lin 2025）

**论文**：*Acoustic Neural 3D Reconstruction Under Pose Drift*, arXiv 2503.08930（CMU）。NeuSIS 的鲁棒化扩展。

**核心**：NeuSIS 等方法**高度依赖精确位姿**，小误差→严重伪影。本法**联合优化神经场（SDF 网络 N、渲染器 M）与 6DoF 声呐位姿**，梯度反传经渲染器到位姿参数。

**位姿参数化**：每个位姿 Tᵢ∈SE(3)，学习一个修正量 (δω,δt)∈R⁶（ω 为 so(3) 轴角），指数映射恢复 δTᵢ，更新 Tᵢ ← Tᵢ·δTᵢ。

**损失**：$L=L_{int}+\lambda_{eik}L_{eik}+\lambda_{reg}L_{reg}$
- 强度损失 L_int（L1）；Eikonal 损失 L_eik（$\|\nabla N\|=1$，平滑正则）；α 的 L1 正则 L_reg。
- ADAM 优化；Marching Cubes 提取零水平集。

**漂移建模**：AUV 靠 DVL+IMU+深度传感器，z/俯仰/滚转有绝对参考（无漂移，仅高斯噪声），而 **x、y、偏航 ψ 靠积分会累积漂移**，建模为带时变漂移的随机过程 $u_{i+1}-u_i\sim N(\Delta u,q_u)$。

**重要发现（位姿不唯一性）**：冻结已收敛的网格、只优化位姿，会收敛到与里程计位姿差异很大、但重建外观相似的一组位姿——即**最小化重建误差的位姿集非唯一**。用 HoloOcean 仿真 + HAUV 水箱真实数据（DIDSON），指标为 Hausdorff 距离。

---

## 11.5 关键辨析：稀疏经典稠密 vs 神经隐式稠密（两种范式关系与定位）

> 常见误解：把神经隐式重建当成"ASfM 稀疏重建之后的加密步骤"。**这是错的。** 二者是同一"稠密/成面"功能位上的**两条竞争范式**，接口在**位姿**而非稀疏点云。本节用于论文相关工作的定位（对应写作日志中的 S1 问题）。

### 两条范式对比

**经典串行管线（本课题所走路线）**：
```
ASfM/BA(稀疏) → 稀疏点云 + 位姿 → 三角化/反投影(点云生成) → 加权泊松等(曲面重建)
```
稀疏点云是"种子"，稠密化建立在稀疏结果之上（类比光学 SfM→MVS）。

**神经隐式管线（NeuSIS/Qadri 2022、Lin 2025）**：
```
带位姿的声呐图像 ──可微声学体渲染──> 直接优化神经 SDF ──Marching Cubes──> 稠密表面
```
**不吃 ASfM 稀疏点云**，直接用"原始声呐图像 + 每帧位姿"最小化渲染强度损失拟合神经隐式表面；稀疏点不参与。

### 二者的真实关系：在"位姿"层耦合，不在"点"层

- NeuSIS 假设**位姿已知且准确**（自身不估位姿），这些位姿来自 ASfM/SLAM/里程计。即 **ASfM → 位姿 → 神经隐式稠密重建**，ASfM 是"位姿供应商"而非"稀疏种子供应商"。
- Lin 2025 更进一步：把**位姿与 SDF 联合优化**，连精确 ASfM 位姿都不强求（可容忍漂移），说明神经隐式在弱化对 ASfM 的依赖。

### 对比表（用于相关工作定位）

| 维度 | 经典（点云→加权泊松，本课题） | 神经隐式（NeuSIS 类） |
|---|---|---|
| 输入 | 稀疏点云 + 位姿 | 声呐图像 + 位姿 |
| 是否依赖稀疏点云 | 是 | 否 |
| 是否需逐场景训练 | 否 | 是（每场景/物体训练数小时） |
| 可解释 / 易注入先验（阴影、置信度） | 强 | 较难 |
| 稠密度 / 平滑度 | 受稀疏点密度限制 | 通常更稠密平滑 |
| 实时性 / 大场景可扩展 | 好 | 弱 |

### 本课题的定位话术（应对"为何不用 NeuSIS"）

神经隐式与本课题的"点云→加权泊松"处于**同一稠密环节**，是替代范式而非后续步骤。选择经典路线的理由：**无需逐场景训练、实时、可解释、便于注入声学阴影/置信度先验**；且本课题的"稀疏几何 + 分割先验"可作为神经方法的**位姿初始化 / 前端**互补。→ 本论文**不实现神经隐式重建**，仅在相关工作中对标定位。

---

## 12. FLS 深度学习 6D 位姿估计（Yu 2026）

**论文**：*Forward-Looking Sonar Based 6D Pose Estimation Using Acoustic-Yolo6D Detection and AnP Inversion*, JMSE 2026（`jmse-14-01014`）。案例：海底采油树（Subsea Christmas Tree）面板。

**核心**：把光学控制点位姿估计重构以适配声学成像，结合声学投影反演：
1. **基于主截断（primary truncation）的 FLS 成像模型**做图像仿真，解决训练数据稀缺（预训练）。
2. **Acoustic-Yolo6D**：多任务声学控制点检测网络，用热图回归 + 辅助目标分割 + 显式距离-方位位置编码，抗散斑噪声、低边界对比、极坐标分辨率变化。
3. **AnP（Acoustic-n-Point）** 模型恢复目标 6D 位姿。

**性能**：平移误差 3.1 cm，姿态误差 10.88°，52 FPS（真实水下声学环境）。

---

## 13. 声呐深度学习综述（Aubard 2025）

**论文**：*Sonar-based Deep Learning in Underwater Robotics: Overview, Robustness and Challenges*, IEEE J. Oceanic Engineering 2025。

**核心**：首个从**鲁棒性（robustness）** 角度的声呐深度学习综述。覆盖分类、目标检测、分割、SLAM 等感知任务模型，系统整理声呐数据集、仿真器、鲁棒性方法（神经网络验证 network verification、分布外 OOD、对抗攻击 adversarial attacks）。

**主要观点**：声呐 DL 研究普遍缺乏鲁棒性；训练数据有限、噪声大；建议建立基准数据集、弥合"仿真到现实（sim-to-real）"鸿沟。

---

## 14. 测深 SfM：两介质折射校正（Dietrich 2017）

**论文**：*Bathymetric Structure-from-Motion*, Earth Surface Processes and Landforms 2017。**注意：这是光学 SfM/摄影测量**（非声呐），用无人机（sUAS）多视立体重建浅溪流河床测深。

**核心问题与本知识库的关联**：跨介质（air/water）成像的**折射校正（refraction correction）**——水下点因光在水/气界面折射而"看起来更浅（apparent depth）"，需按 Snell 定律校正。作者提出对点云中每个"点/相机组合"迭代计算折射校正方程的多相机方法。精度 ~0.02% 飞行高度。

> 与声呐/声光标定专利的联系：两介质几何、Snell 折射建模的思路，可类比声光信息空间配准中的介质界面处理。

---

## 15. 方法对比总表

| 论文 | 年份 | 类别 | 传感器 | 是否需平面假设 | 是否需精确位姿 | 输出 | 关键技术 |
|---|---|---|---|---|---|---|---|
| Huang ASfM | 2015 | 稀疏 SfM/BA | FLS + 里程计 | 否 | 否（联合估计） | 稀疏点+位姿 | 因子图, LM, 退化分析 |
| Huang 数据关联 | 2016 | 数据关联 | FLS | 否 | 否 | 特征对应 | 对应树, χ²门控 |
| Westman 欠约束 | 2018 | 特征 SLAM | FLS | 否 | 否 | 稀疏点+位姿 | A-KAZE, 特征值判据, 非/半参数 |
| Lin 特征检测 | — | 前处理 | FLS | 否 | — | 特征/匹配 | CFAR, DBSCAN, SSL |
| Westman Fermat | 2020 | 表面重建 | FLS | 否 | 需(里程计) | 表面点 | Fermat流, z轴平移差分 |
| DISO (Xu) | 2024 | 直接里程计 | FLS+DVL+IMU | 是(零俯仰) | 估计 | 位姿+稀疏图 | 强度误差, 窗口BA |
| Dong SIO | 2022 | 声惯性里程计 | FLS+IMU | 否 | 估计 | 位姿+特征 | IMU预积分, 重投影因子 |
| Huang 多波束 | 2026 | 里程计 | 多波束FLS | 否 | 估计 | 6DOF位姿 | 声学PnP, 运动方向先验 |
| NeuSIS (Qadri) | 2022 | 稠密神经重建 | FLS | 否 | **需** | SDF表面 | 可微体渲染, 神经隐式 |
| Lin 位姿漂移 | 2025 | 稠密神经重建 | FLS+DVL | 否 | 否（联合优化） | SDF表面+位姿 | 联合位姿优化, 漂移建模 |
| Yu 6D位姿 | 2026 | 深度学习位姿 | FLS | 否 | 估计 | 6D位姿 | Acoustic-Yolo6D, AnP |
| Aubard 综述 | 2025 | 综述 | 声呐 | — | — | — | DL鲁棒性 |
| Dietrich 测深 | 2017 | 光学SfM | 相机(UAV) | — | — | 河床测深 | 折射校正, 两介质 |

**技术演进脉络**：
- 成对匹配（平面假设）→ **多视 ASfM（无平面假设，因子图 BA）**（2015）→ 自动数据关联（2016）→ 欠约束路标处理（2018）→ Fermat 路径稠密化（2020）
- 里程计线：特征法/ICP → **直接法 DISO**（2024）；紧耦合 **声惯性 SIO**（2022）；**声学 PnP + 全局 BA**（2026）
- 稠密重建线：空间雕刻/体素 → **神经隐式 NeuSIS**（2022）→ **联合位姿优化抗漂移**（2025）
- 感知/位姿线：**深度学习 6D 位姿**（2026）、DL 鲁棒性综述（2025）

---

## 16. 关键公式速查

**1. 球↔笛卡尔**：$C=r[\cos\psi\cos\theta,\sin\psi\cos\theta,\sin\theta]^T$

**2. 声呐测量（丢俯仰）**：$r=\sqrt{x_s^2+y_s^2+z_s^2},\ \psi=\arctan2(y_s,x_s)$

**3. ASfM 非线性最小二乘**：
$$\Theta^*=\arg\min\Big[\|x_0\|^2_\Lambda+\sum\|h(x_i,l_j)-m_k\|^2_{\Xi_k}+\sum\|g(x_{i-1},x_i)-u_i\|^2_{\Lambda_i}\Big]$$

**4. 可解性**：$6(n-1)+3m\le 2mn$

**5. 良约束判据**（Westman）：$\lambda_2/\lambda_3<\rho$（$A^TA$ 特征值）

**6. CFAR 阈值**：$t=\alpha(\sum_N I-\sum_G I)/n,\ \alpha=n(P_{fa}^{-1/n}-1)$

**7. Fermat 流方程**：$x_F=c-r_F\nabla_c r_F(c)$

**8. DISO 强度残差**：$E_I=\sum\|I_c(^{I_c}p_i)-I_r(^{I_r}p_i)\|^2$

**9. 声呐投影(η法, Dong)**：$\eta=1/\cos\theta,\ [p_c;0]=K P_c$

**10. NeuSIS 图像形成**：$I_p=\int_{\phi_{min}}^{\phi_{max}}\int_{r_i-\epsilon}^{r_i+\epsilon}\frac{E_e}{r}T\sigma\,dr\,d\phi$

**11. 位姿修正指数映射(Lin 2025)**：$T_i\leftarrow T_i\cdot\delta T_i,\ \delta T_i=\exp([\delta\omega]_\times,\delta t)$

---

## 17. 术语表

| 英文 | 中文 | 说明 |
|---|---|---|
| FLS (Forward-Looking Sonar) | 前视声呐/成像声呐 | 测方位+距离，丢俯仰角 |
| Elevation ambiguity | 俯仰角模糊 | 声呐三维重建根本难题 |
| Elevation arc | 俯仰弧 | 投影到同一像素的3D点集合 |
| ASfM | 声学结构光运动恢复 | 声呐版 SfM |
| Bundle Adjustment (BA) | 束调整 | 联合优化位姿与结构 |
| Factor graph | 因子图 | 变量节点+因子节点的二部图 |
| Frustum | 视锥 | 声呐视场空间 |
| A-KAZE | — | 非线性尺度空间特征，抗散斑噪声 |
| CFAR | 恒虚警率 | 自适应阈值检测/去噪 |
| Space carving | 空间雕刻 | 用最近回波"雕"出自由空间 |
| Fermat path | 费马路径 | 距离函数极值/鞍点表面点 |
| Direct method | 直接法 | 最小化强度误差(不提特征) |
| Preintegration | 预积分 | IMU 帧间相对约束 |
| PnP / AnP | n点透视/声学n点 | 已知3D-2D对应求位姿 |
| SDF | 有符号距离函数 | 神经隐式表面表示 |
| NeuSIS | — | 神经隐式声呐重建 |
| DVL | 多普勒测速仪 | 提供对地三轴速度 |
| Dead reckoning | 航位推算 | 惯性积分定位(会漂移) |
| Dual-pose ambiguity | 双位姿模糊 | 俯仰不确定导致的对称解 |
| LM / Gauss-Newton | 列文伯格-马夸尔特/高斯牛顿 | 非线性最小二乘求解 |
| ATE / RPE | 绝对轨迹误差/相对位姿误差 | 里程计精度指标 |
| Hausdorff distance | 豪斯多夫距离 | 重建网格精度指标 |
| Refraction correction | 折射校正 | 两介质(air/water)成像 |

---

*说明：`Zhang - Adding Conditional Control to Text-to-Image Diffusion Models`（ControlNet）与声呐无关，未纳入；`simulated.pdf` 为扫描图无可提取文本。*

---

## 18. 声呐物理原理详解

> 综合各论文（Huang 2015、Westman 2018/2020、DISO、NeuSIS、Aubard 综述）对成像声呐传感器原理的描述整理。

### 18.1 成像声呐是什么（主动声学传感器）

成像声呐（imaging sonar / 前视声呐 FLS，又称声学相机 acoustic camera）是**主动式声学传感器**：
1. **发射（emit）**：向前方一个锥形空间（frustum，视锥）发射脉冲声波（acoustic pulse / ping）。
2. **传播与反射**：声波在水中传播，遇到物体表面反射（回波 echo）。
3. **接收（receive）**：用**一维换能器阵列（1-D transducer array）** 接收反射声波的强度。
4. **成图**：把回波强度按（方位角，距离）组织成二维图像。

**为何水下用声呐而非相机**：光在水中衰减快、浑浊水（turbid water）散射严重，光学相机有效距离常仅 1–2 m；声波在浑水中传播损耗小、作用距离远（几十米以上），且对水质不敏感。代价是：**分辨率低、信噪比低（low SNR）、噪声/伪影多**。

### 18.2 三个测量维度：距离、方位角可测，俯仰角丢失

- **距离 r（range）**：由声波**飞行时间（time of flight, ToF）** × 已知水中声速（约 1500 m/s）/2 求得。对应图像的行（range bins）。
- **方位角 ψ / θ（bearing / azimuth）**：由阵列中不同换能器接收同一回波的**时间差 / 相位差**经**波束形成（beamforming）** 解算。精度通常优于 1°。对应图像的列（bearing bins）。
- **俯仰角 φ / θ（elevation）**：**丢失**。原因——一维阵列只能在方位方向分辨来向，在俯仰方向没有分辨能力；换能器在俯仰方向有一定开角（elevation aperture / FOV），该开角内**同一（方位, 距离）的所有回波被累加到同一像素**。

> 记号提示：不同论文对 elevation 用 θ 或 φ、对 bearing 用 ψ 或 θ，需按上下文区分。本库总体沿用 Huang：ψ=方位，θ=俯仰。

### 18.3 俯仰弧（elevation arc）与俯仰角模糊

对图像中一个像素 (ψ, r)，真实 3D 点可位于以声呐为心、半径 r、方位 ψ 固定、俯仰角 φ∈[φmin, φmax] 变化的一段**圆弧（俯仰弧）** 上的任意位置。这就是**俯仰角模糊（elevation ambiguity）**——三维重建的根本难题，等价于"从多视观测中恢复每个像素的俯仰角"。

### 18.4 图像形成模型（intensity image formation）

一个像素强度 = 该（方位, 距离）下俯仰弧上所有反射体反射声能的**累加/积分**（Aykin、Qadri/NeuSIS）：

$$ I_i(r;c)=\int_X f(x;c)\,\delta\big(r-r(x;c)\big)\,dx $$

其中 f(x;c) 综合了**可见性、反射率（reflectivity）、着色/明暗（shading）、声波空间传播**等因素。NeuSIS 进一步写成沿俯仰弧和距离窗的双重积分（见 §10）。

**强度受哪些物理量影响（DISO 的观察，直接法的物理依据）**：
- 目标表面的**反射系数**（材质）
- 表面**法向朝向**相对声呐的角度（镜面 vs 漫反射）
- 到声呐的**距离**（声能随距离衰减 attenuation）
- 是否被**遮挡（occlusion）**

→ 因此强度在短时间内相对稳定（可作直接法约束），但长时间/大运动下会显著波动（故 DISO 后端改用重投影误差）。

### 18.5 声呐特有的噪声与伪影

- **散斑噪声（speckle noise）**：相干声波干涉造成的颗粒状噪声，是声呐图像最主要噪声，普通线性滤波难去除（故用 A-KAZE 非线性扩散、CFAR）。
- **多径效应（multipath）**：声波经多次反射后返回，在更远距离处产生虚假强度。NeuSIS/仿真常显式建模。
- **阴影区（shadow zone）**：近处物体遮挡后方，形成低强度区——**低强度 ≠ 无障碍**。阴影也可反过来提供几何线索（Aykin 用阴影恢复俯仰）。
- **低分辨率 / 低信噪比**：使 ORB、SIFT 等光学特征失效，需专门方法。

### 18.6 极坐标图像 vs 笛卡尔图像

声呐原始数据是**极坐标图像（polar image）**：x 轴=方位角 θ、y 轴=距离 r。使用/显示前常转成**笛卡尔图像（Cartesian image）**。DISO 给出的相似变换（similarity transform）：

$$ ^Ip=S_{IC}\,{}^Cp=\begin{bmatrix}s\cos\omega&-s\sin\omega&t_x\\ s\sin\omega&s\cos\omega&t_y\\0&0&1\end{bmatrix}\begin{bmatrix}x\\y\\1\end{bmatrix} $$

其中 s=h/r 为尺度，ω 为旋转角，tx=w/2, ty=h/2。笛卡尔坐标 $C_p=[r\cos\theta, r\sin\theta]^T$。

### 18.7 剖面模式 vs 成像模式（俯仰视场的权衡）

- **剖面模式（profiling mode）**：加特殊镜头使俯仰 FOV 极小（可至 1°），此时可近似 θ≈0、误差小，但成像体积小、帧间重叠少（不利于多视约束）。
- **成像模式（imaging mode）**：加**扩束镜（spreader lens）** 使俯仰 FOV 大（如 28°），成像体积大、重叠多，但俯仰模糊严重——ASfM/Fermat 等正是为解决大俯仰视场下的重建而设计。

---

## 19. 声呐图像坐标与像素映射细节

### 19.1 DIDSON 像素映射（Huang 2015 完整公式）

设笛卡尔声呐图像中特征像素坐标 (u,v)，γ 为每米像素数常数，w=96 方位 bin，h=512 距离 bin：

$$ \gamma=\frac{w}{2\,r_{max}\sin(\psi_{max}/2)} $$
$$ x_s=\frac{u-w/2}{\gamma},\quad y_s=\frac{r_{max}-v}{\gamma} $$
$$ r=\sqrt{x_s^2+y_s^2},\quad \psi=\frac{180}{\pi}\arctan2(x_s,y_s) $$

**离散化到 bin**：
$$ n_r=\frac{h(r-r_{min})}{r_{max}-r_{min}}\quad(\text{距离 bin}) $$
$$ n_b=M_4(w,\psi)\quad(\text{方位 bin，厂商给的三阶多项式，4个系数，含镜头畸变校正}) $$

> 要点：真实声呐的方位→像素映射**非线性**（镜头畸变），厂商用三阶多项式 M₄ 描述。工程中若无厂商多项式，可像本项目那样**线性自标定** beam=A·θ+B、range=C·ρ+D（见 §21）。

### 19.2 线性投影近似（工程常用）

许多实现（含本项目 12.5新.py）把方位/距离到像素近似为线性：

$$ u=c_x+f_x\cdot\psi,\qquad v=c_y+f_y\cdot r $$

即用"声呐内参" {fx, fy, cx, cy} 类比相机内参。适合窄视场、无厂商畸变表时的快速建模。

---

## 20. 各论文实现细节与设计思想

### 20.1 Huang ASfM（2015/2016）——因子图 + 增量平滑

**参数化与初始化**
- 位姿 6 DOF（x,y,z + 姿态），点 3 DOF；旋转过参数化，用**指数映射（exponential map）** 做局部最小参数化，更新算子 ⊕（一般是加法，旋转用指数映射）。
- 路标初值：取**首次观测**的 (ψ, r)，设 θ=0 反投影到局部笛卡尔，再用基准位姿转世界坐标。

**求解（非线性最小二乘）**
- 每次迭代在当前估计 Θ 处线性化：$\arg\min_\Delta\|A\Delta-b\|^2$，A∈R^{M×N}，M=6n+2m（测量行），N=6n+3m（未知量）。
- **协方差白化**：把 Σ^{-1/2} 吸收进 A 的对应块行，$\|\Delta\|^2_\Sigma=\|\Sigma^{-1/2}\Delta\|^2$。
- 正规方程 $A^TA\Delta=A^Tb$，Cholesky 分解 $A^TA=R^TR$，前代/回代求 Δ。用 **iSAM**（[11]）做增量式高效求解。
- 首位姿用**一元先验因子 p** 固定，消除规范（gauge）自由度。

**思想精髓**：把 SLAM 后端的成熟工具（因子图、平滑、iSAM）迁移到声呐，用**多视几何约束 + 里程计约束**共同解俯仰模糊，避免平面假设。

### 20.2 Huang 增量数据关联（2016）——树搜索 + χ² 门控

- **门控剪枝**：新特征来时，把历史路标重投影到当前帧；仅观测一次的路标沿俯仰弧按 1° 采样后逐个重投影，落入门限（0.2 m 距离、4°方位）者为候选。
- **对应树 + 假设评分**：类 JCBB，每层一个观测（含 null=新路标），路径=一个关联假设。按 χ²_{d,α} 检验（α=0.99）接受，**惩罚 null 数**（优先少 null、残差小）。
- **两个关键工程技巧**：① 过近特征（<1°或<0.2m）直接丢弃避免歧义；② 测试假设时用**批量建图**而非增量，避免局部极小（新观测能解开旧歧义）。

### 20.3 Westman 欠约束路标（2018）——分层建模

- **特征前端**：A-KAZE（非线性尺度空间抗散斑），几何投影数据关联（Algorithm 1，阈值 D1/D2 像素，保守匹配，无重复分配）。
- **相对球坐标参数化**：路标以相对"**基准位姿 base pose**（首次观测帧）"的 (ψ, r, θ) 表示，比全局笛卡尔**更线性**、更稳定（本项目 12.5新.py 正是此法）。
- **可观测性判据**：对单路标做线性化，检查 3×3 的 $A^TA$ 特征值 λ₁≥λ₂≥λ₃；俯仰完全不可约束时秩亏（λ₃→0）。判据 $\lambda_2/\lambda_3<\rho$（如 ρ=20）→ 良约束；否则欠约束。**只看线性化点的特征分解，不真正求解该子问题**。
- **欠约束两法**：
  - 非参数：移出状态向量，因子内**网格搜索俯仰角** θ*∈{θmin,...,θmax}（Δθ=(θmax−θmin)/60）取最小重投影，等价均匀分布信念，防止俯仰跑出视场。
  - 半参数：只移俯仰，把 (ψ,r) 建模为正态、可被优化。
- 可**升级（upgrade）**：欠约束路标一旦满足判据，删旧节点/因子、以 3-DOF 重加入。
- 工具：**GTSAM + LM**。

**思想精髓**：不是所有路标都能三角化；**强行三角化欠约束路标会污染位姿**。识别并区别对待，才能既定位准又能重建可重建的点。

### 20.4 Fermat 路径（2020）——差分立体，绕开强度依赖

- **按方位列独立处理**：每个方位角 θᵢ 对应一个含 z 轴的 2D 平面 πᵢ，在平面内解 2D 问题。
- **运动策略**：沿声呐 **z 轴平移**，使各列的方位平面不变，可同时估计所有列的 ∂r_F/∂z。
- **梯度估计抗噪**：对 Fermat 距离随 z 的变化拟合二次多项式 $r_F(z)=a_2z^2+a_1z+a_0$，$\partial r_F/\partial z=2a_2z+a_1$（等价平滑/曲率先验）。
- **不连续检测**：Fermat 路径在图像列中是强度**不连续点**，用 **Canny 边缘检测** + 启发式去伪。
- **边界点**：多视非线性最小二乘 $\min_b\sum_j(r_b(c_j)-\|b-c_j\|)^2$。

**思想精髓**：不依赖精确强度值/反射率（只用不连续位置），把俯仰恢复转化为"2D 距离-only 建图"，与 NLOS 成像同源；是空间雕刻的推广（能重建凹面）。

### 20.5 DISO 直接法（2024）——强度误差 + 金字塔 + 窗口 BA

- **关键点选择**：极→笛卡尔后 Sobel 算强度梯度，阈值筛候选，**网格滤波**每格取梯度最大者（分布均匀），取 top N。
- **强度残差 Jacobian**（对 SE(3)，旋转用 retraction）：
  $$ \frac{\partial r_I}{\partial R}=-\frac{\partial I_c}{\partial {}^{I_c}p}\,s\,R_{IC}\begin{bmatrix}1&0&0\\0&1&0\end{bmatrix}[R_{S_cS_r}{}^{S_r}p_i]_\wedge,\quad \frac{\partial r_I}{\partial p}=\frac{\partial I_c}{\partial {}^{I_c}p}R_{IC}\begin{bmatrix}1&0&0\\0&1&0\end{bmatrix} $$
  其中 ∂I_c/∂p 是图像强度梯度。
- **粗到精**：多尺度金字塔，缓解低帧率下直接法对初值敏感；IMU/DVL(EKF) 提供初值。
- **三级跟踪**：帧到帧 → 帧到窗口（抗漂移、按强度误差剔外点）→ 窗口优化。
- **后端为何改用重投影误差而非强度**：① 窗口时间长、运动大，强度不稳定；② 重投影免去每次重采样图像，可纳入更多关键帧。
- 多线程前/后端（类视觉 SLAM）。

**思想精髓**：跳过"特征检测+描述子匹配"（声呐上易失败），直接用高梯度点的强度一致性；但在需要长期一致的后端回到几何（重投影）约束。

### 20.6 SIO 声惯性（2022）——预积分 + 解析 Jacobian

- **IMU 预积分**：只在声呐帧间做相对约束（15 维残差：位置/姿态/速度/加计零偏/陀螺零偏），避免每次优化后重积分（Forster 方法）。
- **声呐投影用 η 技巧**：$\eta=1/\cos\theta$，$[p_c;0]=K P_c$，把丢俯仰的投影写成矩阵形式。
- **重投影残差**：$e=[r_k-\hat r_k,\ \psi_k-\hat\psi_k]^T$，给出**完整解析 Jacobian**（链式：∂e/∂P_s·∂P_s/∂P_c·∂P_c/∂位姿增量，含叉乘算子 [·]×）。
- 初值：IMU 航位推算给帧间运动 + 首帧反投影给特征。工具 **Ceres**。

### 20.7 NeuSIS 神经隐式（2022）——可微声学体渲染

- **双网络**：SDF 网络 N(x) 输出到最近面的有符号距离；辐射网络 M(x) 输出该点出射声辐射。
- **渲染离散化**：沿俯仰弧采样点集 A_p，每点再沿声学射线采样算透射率/不透明度：
  $$ \hat I_p=\sum_{x\in A_p}\frac{1}{r(x)}T[x]\,\alpha[x]\,M(x) $$
  $$ \alpha[x_s]=\max\Big(\frac{\Phi_s(N(x_s))-\Phi_s(N(x_{s+1}))}{\Phi_s(N(x_s))},0\Big),\quad T[x_s]=\prod_{r<s}(1-\alpha[x_r]) $$
  Φ_s 为 sigmoid、s 可训练。
- **损失**：渲染强度 L1 + 正则；**Marching Cubes** 提零水平集得网格。

**思想精髓**：把 NeRF/NeuS 的可微渲染换成"声学波前传播"的前向模型，用隐式 SDF 表示，避免体素的内存爆炸，且无监督。

### 20.8 Lin 位姿漂移（2025）——联合优化位姿与神经场

- **位姿作为可学习参数**：每帧位姿 T∈SE(3) 附加修正量 (δω,δt)∈se(3)（δω 轴角），指数映射得 δT，更新 T←T·δT，梯度经渲染器反传到位姿。
- **损失**：$L=L_{int}+\lambda_{eik}L_{eik}+\lambda_{reg}L_{reg}$（强度L1 + Eikonal 平滑 + α 的 L1）。ADAM。
- **漂移建模**：DVL+IMU+深度计使 z/俯仰/滚转有绝对参考（仅高斯噪声），而 **x,y,偏航靠积分累积漂移**：$u_{i+1}-u_i\sim N(\Delta u,q_u)$，只对这三轴注入漂移噪声做实验。
- **重要结论**：最小化重建误差的位姿集**非唯一**（冻结网格只优化位姿会收敛到远离里程计但重建相似的解）。

### 20.9 Huang 多波束（2026）——声学 PnP + 运动方向先验

- **三段式**：2D-2D 两视图声学 BA 初始化 → 3D-2D **声学 PnP（AnP）** → 全局 BA。
- **对称双位姿模糊**：俯仰不确定使声呐对极几何出现两个对称解；用**运动方向先验（motion direction prior）** 选正确解。
- **思想**：声呐投影非线性使闭式解失效，改用**基于图的优化**；全局 BA 用满历史观测提精度。

---

## 21. 工程实现要点与调参经验

> 结合本项目 `12.5新.py`（前端相对球坐标 SfM）与 `ba_optimize.py` / `ba.cs`（后端 BA）实现，对应论文思想的落地。

### 21.1 残差项设计（本项目 BA）

四类残差（对应因子图四类因子）：
1. **首帧位姿先验**（强权重，如 1000）：固定第 0 帧，消除规范自由度（对应 Huang 的一元先验因子 p）。
2. **里程计相对位姿约束**（权重~100）：相邻关键帧相对位姿 $T_{rel}=T_k^{-1}T_{k+1}$，残差用 $\Delta=T_{meas}^{-1}T_{rel}$ 的平移 + 旋转向量（对数映射）。既信任整体形状又允许 BA 修正。
3. **声呐重投影残差**（权重 1，像素量纲）：路标世界坐标 → 载体系 $P_b=R_f^T(P_w-t_f)$ → (θ,ρ) → 经标定映射到 (beam, range) 像素，残差以像素计。
4. **路标弱先验**：拉向初始点云，稳定仅单帧可见（欠约束）的路标（对应 Westman 欠约束处理的工程简化版）。

### 21.2 关键工程技巧

- **残差改到像素量纲**：用 tracks.csv 自标定 beam=A·θ+B、range=C·ρ+D（本项目 A≈249.9, C≈277.0），使 Huber 阈值有物理意义、声呐项能主导优化。
- **Huber 鲁棒核**（δ=20 px）：对每个观测按残差范数 IRLS 降权，抑制误匹配/里程计粗差。scipy `least_squares(method="trf", loss="huber", f_scale=δ)`。
- **相对球坐标参数化**（12.5新.py）：路标 = 相对基准帧的 (θ, φ, r)，更线性、更稳定（Westman 思想）；优化后再转世界 XYZ。
- **旋转的稳健转换**：`rot_to_vec` 处理 θ≈0 与 θ≈π 的奇异；欧拉角约定统一 R=Rz·Ry·Rx。
- **视场裁剪**：投影落在俯仰视场 [θmin,θmax] 外时残差置零/该观测无效。

### 21.3 求解器选择

- 稠密小规模：scipy `least_squares`（`lm` 或 `trf`+huber）。
- 大规模：应利用**稀疏法方程 + Cholesky**（本项目 C# 版 ba.cs 用 LM + 稀疏 + Cholesky），对应 Huang 的 iSAM/GTSAM 思路。

### 21.4 结果解读经验（来自 report.md）

- 重投影 RMS 5.31 px → 1.02 px（亚像素级）即位姿/点云一致性显著提升。
- **路标最大位移集中在 Z（俯仰）方向属正常**——声呐单帧不测俯仰，是弱可观测的必然现象。
- Huber 外点 0 表明数据关联干净。

### 21.5 可改进方向（论文对照）

| 改进点 | 对应论文思想 |
|---|---|
| 观测按协方差加权（σ_θ 波束分辨率、σ_ρ 距离bin）做信息矩阵白化 | Huang 协方差白化 §20.1 |
| 收紧 Huber / 加 χ² 门限做外点剔除迭代 | Huang 数据关联 χ² §20.2 |
| 用满非关键帧观测（当前仅~11%）、里程计插值初始化位姿 | 提升约束数 |
| track→landmark 由上游直接给出，避免重投影反推 | 消除关联错误 |
| 引入海底平面/几何先验约束弱可观测 Z 方向 | 欠约束路标处理 §20.3 |
| 输出信息矩阵→位姿/路标协方差与精度评估 | 可观测性/不确定度分析 §20.3 |
