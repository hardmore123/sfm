# BA 算法改进实验日志（改进 1 & 2）

> 记录 `BA算法自身改进.md` 中改进点 1（相对球坐标参数化）与改进点 2（欠约束路标识别+分类建模）的实现、测试、前后对照数据与声呐专家视角的机理分析。
> 相关文件：`ba_improve.py`（实现+测试+绘图）、`BA自身改进_测试结果.md`（数据报告）、`ba_improve_cloud.png` / `ba_improve_zdrift.png` / `ba_improve_metrics.png`（效果图）。
> 日期：2026-08-27

---

## 0. 一句话总结

在不牺牲实质精度（重投影仍亚像素/像素级）的前提下，把声呐 BA 最典型的病态——欠约束路标沿俯仰弧乱跑——从平均 0.27 m 压到 0.08 m（↓70%），并用"0 个良约束路标"这一结果量化证明了当前运动下俯仰几何不可观测，为大论文"需要阴影先验补俯仰"提供实证支点。

---

## 1. 改进动机：问题的物理根源

前视声呐（FLS）单帧只测**方位角 ψ**和**斜距 ρ**两维，**俯仰角不可观测**——同一像素对应一整条"俯仰弧"上的所有 3D 点。在世界笛卡尔参数化下，路标 Z 坐标（近似对应俯仰方向）就是弧上的自由维度。

基线 BA（`ba_optimize.py`）的两个设计放大了病态：

1. **世界笛卡尔 (x,y,z)**：把本质是"沿俯仰弧滑动"的角度不确定性硬塞进笛卡尔 Z 轴，代价函数对 Z 近似平坦（`AᵀA` 该方向特征值趋零）。
2. **所有路标一视同仁当 3-DOF + 弱先验**：弱先验压不住病态方向，欠约束点 Z 在弧上自由滑动去迎合每条重投影观测——即 Westman 2018 的"强行三角化欠约束路标会污染估计"。

---

## 2. 改进内容（机理层面）

- **改进 1（相对球坐标）**：路标改用相对基准帧的 (ψ, r, elev)，把不可观测的自由度**显式隔离成独立参数 elev**。病态不再泄漏到笛卡尔三轴耦合，而是集中在一个可单独处理（约束/搜索/先验）的角度变量上；同时与前端 `12.5新.py` 参数化统一。
- **改进 2（欠约束识别 + 分类建模）**：对每个路标用其观测算单点 `JᵀJ`(3×3) 特征值，判据 **λ₂/λ₃ < ρ(=20)** 分良/欠约束；欠约束者把 elev **移出自由优化**，改为在物理仰角孔径内网格搜索 + 交替再拟合（坐标下降 4 轮）。核心：**不让优化器用物理不可信的自由度去降残差。**

**实现要点**：新建 `ba_improve.py`，不改动 `ba_optimize.py`（基线保留作 fallback）；复用基线的数据加载、track→landmark 反推、像素自标定、里程计，保证三方对照口径一致。

---

## 3. 测试配置

- 关键帧 10，初始路标 217，参与 BA 观测 1641，成功关联 200。
- 像素标定：`beam=249.867·θ+127.504`，`range=277.041·ρ−3.000`。
- Huber δ=20px；仰角孔径设 ±0.30 rad，网格 61 点；外迭代 4 轮。
- 环境：Python 3.14、numpy 2.5.1、scipy 1.18.0。

---

## 4. 前后对照数据

| 指标 | 优化前 | 基线 BA | 改进 BA | 改进 vs 基线 |
|---|---|---|---|---|
| 重投影 RMS (px) | 5.2934 | 1.0137 | 1.2141 | ↑约0.2px（取舍） |
| 重投影 mean (px) | 3.9557 | 0.8227 | 0.9819 | ↑（同上） |
| 方位 θ RMS (rad) | 0.01984 | 0.00383 | 0.00406 | 基本持平 |
| 斜距 ρ RMS (m) | 0.00613 | 0.00272 | 0.00344 | 略升 |
| 路标总位移 mean (m) | — | 0.277 | 0.091 | **↓67.3%** |
| 路标总位移 max (m) | — | 2.240 | 1.760 | ↓21.4% |
| **路标 Z 位移 mean (m)** | — | 0.269 | 0.082 | **↓69.5%** |
| **路标 Z 位移 max (m)** | — | 1.928 | 1.409 | ↓26.9% |

改进 BA 外迭代收敛：代价 1565.6 → 1427.4 → 1342.5 → 1293.5，RMS 0.9106 → 0.8695 → 0.8433 → 0.8277。

### 欠约束分类结果（判据 λ₂/λ₃ < 20）

| 类别 | 数量 |
|---|---|
| 良约束 (well-constrained) | 0 |
| 欠约束 (under-constrained) | 198 |
| 单帧可见 (天然欠约束) | 19 |

λ₂/λ₃ 分布（有限值）：min=25.69，中位数=56.79，max=1913.03。

---

## 5. 逐项数据解读（声呐专家视角）

### 5.1 重投影 RMS 那 0.2px 升高不是退步

基线能到 1.01px，是因为它允许每个欠约束点的 Z 在俯仰弧上自由滑动去"贴合"观测——但这个滑动量在物理上**虚假**（对应 report.md 的 2.25m Z 乱跑）。改进版把 elev 锁在物理孔径内，禁止了"用不可信自由度换低残差"，所以残差略回升到 1.21px，换来 Z 向乱跑下降 70%。

> 关键认知：**重投影残差低不代表重建对**——俯仰弧上任意一点重投影都一样低。基线是"重投影漂亮但几何不可信"，改进是"重投影略松但几何可信、位姿不被污染"。对水下三维重建，后者才是目标。

### 5.2 方位/斜距几乎不变

θ RMS 0.00383→0.00406、ρ RMS 0.0027→0.0034，变化极小。方位和斜距本就是声呐的**强可观测量**，改进不触碰它们；被约束的只是弱可观测的俯仰方向。

### 5.3 核心收益——抑制俯仰(Z)乱跑

总位移 mean ↓67%、Z 位移 mean ↓70%。点云整体稳定得多，欠约束点不再沿弧滑出。这是 Westman 2018 分类建模思想的直接落地验证。

---

## 6. 最重要的发现：0 个良约束路标

判据 λ₂/λ₃<20 下，198 个多帧路标**全部**判为欠约束（最小比值 25.69）。这不是 bug，而是有价值的结论：

- 说明**本数据集运动多样性不足**——轨迹主要是前向平移 + 小幅偏航（见 `12.5新.py` 里程计增量，Z 平移和俯仰变化都很小），正落在 Huang 2015 的**退化运动区间**（纯 X 平移、偏航+Y 平移下俯仰不可恢复）。
- 即**当前采集轨迹从原理上就无法靠多视几何约束住俯仰**。这正是大论文创新点二（声学阴影→高度反演）、改进 5（视场约束）、改进 6（阴影/强度）的**直接实证依据**：几何这条腿在这种运动下补不了俯仰，必须引入阴影先验这条腿。

> 该发现比单纯指标改善更有论文价值——用数据证明了"为什么纯多视几何不够"。

---

## 7. 效果图解读

- **`ba_improve_cloud.png` 侧视 XZ（最直观）**：基线（红）在 Z 方向铺得开、有离群点飞出；改进（蓝）在 Z 方向更收拢、贴近初始结构——俯仰乱跑被抑制的视觉证据。
- **`ba_improve_zdrift.png`**：直方图基线红色长尾 vs 改进蓝色集中；排序曲线蓝线整体压在红线下；散点图绝大多数落在对角线下方（改进位移更小）。
- **`ba_improve_metrics.png`**：左图重投影两者接近（基线略低），右图漂移改进全面更低——并排讲清"精度持平、几何更稳"的取舍。

---

## 8. 局限与诚实说明

1. **Z max 仍有 1.41m**：仰角孔径限制的是**角度**（±0.3rad）不是绝对 Z，远距离路标 `r·sin(elev)` 仍可较大——物理上正确，非缺陷。
2. **缺真值**：目前只能用重投影一致性 + 位移稳定性侧证，无 Z 方向绝对精度真值。要断言"改进后更接近真实几何"，需仿真高度真值或受控靶（对应大论文 6.4 真值方案）。
3. **仰角孔径 ±0.3rad 为设定值**：真实值应由 `sensor_calib.yaml` 仰角孔径给出（对应上游对接 P2 项），此处为合理假设。
4. **半参数 vs 非参数**：采用"移出仰角 + 孔径内搜索"的半参数实现（Westman Method 2 精神），比每次迭代完整网格搜索的非参数 Method 1 更快；仰角逐外迭代刷新而非连续优化，属工程折中。

---

## 9. 结论与下一步

**结论**：改进在不牺牲实质精度的前提下，把欠约束路标沿俯仰弧乱跑从 0.27m 压到 0.08m（↓70%），并量化证明当前运动下俯仰不可观测。前者是鲁棒性直接提升，后者是大论文"需要阴影先验补俯仰"的实证支点。

**下一步**（承接 `BA算法自身改进.md` 优先级）：
1. 改进 5：视场箱式硬约束（`trf`+bounds 把 elev 限在物理孔径），配合 λ₃ 可观测性诊断。
2. 创新点二：声学阴影→高度反演，把仰角先验以带不确定度的软观测注入 BA——从"限制俯仰乱跑"走向"真正测量俯仰"。
3. 补真值方案（仿真高度真值/受控靶），给出 Z 向绝对精度的定量结论。

---

## 10. 复现

```bash
python ba_improve.py
```
输出：`BA自身改进_测试结果.md` + `landmarks_improved.npy/.ply` + 三张对照图 + 终端三方对照表。

---

## 附：方法对应论文

- 改进 1 相对球坐标：Westman 2018 §III-B / Huang 2016——声呐路标相对基准帧球坐标比世界笛卡尔更线性。
- 改进 2 欠约束识别：Westman 2018——`JᵀJ` 特征值判据 λ₂/λ₃<ρ；欠约束半参数（仰角移出、孔径内搜索）。
- 退化运动/可观测性：Huang & Kaess 2015——纯 X 平移、偏航+Y 平移下俯仰不可恢复。


---

# 改进 3 & 4 实验记录（2026-08-28）

> 对应 `BA算法自身改进.md`：改进 3（解析雅可比 + 稀疏求解）与改进 4（GNC + χ² 外点剔除）。
> 实现与测试：`ba_improve34.py`；报告：`BA自身改进34_测试结果.md`；效果图：`ba_improve34_gnc.png`。

## 11. 改进 3：解析雅可比与稀疏求解

### 11.1 实现内容

基线 `ba_optimize.py` 使用 `scipy.least_squares(trf)` 的数值稠密雅可比。改进版完成：

1. 推导声呐重投影 `(beam, range)` 对路标和位姿的解析导数；
2. 对 `P_b = Rᵀ(P_w - t)`，利用平移雅可比 `J_t = -J_l`；
3. 对 `R=Rz(yaw)Ry(pitch)Rx(roll)` 使用欧拉角解析旋转导数；
4. 将声呐因子、位姿因子、路标先验按 BA 二部图结构组织为稀疏 CSR 矩阵；
5. 对声呐雅可比进行向量化，减少 Python 循环开销；
6. 里程计块暂用局部有限差分，只扰动相邻两个位姿的 12 个变量，作为工程折中。

解析雅可比通过有限差分校验：最大绝对误差约 `3.7e-4`，满足当前数值尺度下的实现验证要求。

### 11.2 同数据对照

测试数据：10 个关键帧、217 个路标、1641 条参与 BA 的观测；三种模式使用完全相同的初始化、关联、标定、里程计和 Huber 参数。

| Jacobian 模式 | 用时 | nfev | njev | 最终 cost | 重投影 RMS |
|---|---:|---:|---:|---:|---:|
| 数值稠密（基线） | 73.34 s | 54 | 45 | 994.929 | 1.0137 px |
| 数值 + 稀疏模式 | 22.21 s | 500 | 497 | 994.956 | 1.0145 px |
| **解析 + 稀疏** | **12.28 s** | 500 | 499 | 994.956 | 1.0145 px |

解析稀疏相对数值稠密加速约 **6.0 倍**。不同运行受系统负载影响，先前测试曾出现约 6–10 倍加速；应在论文中报告单次实际运行或多次运行均值，不宜只报告最有利的一次。

### 11.3 结果解释

- 最终 cost 和重投影 RMS 基本一致，说明稀疏化没有改变优化目标，解析雅可比没有造成可见精度损失。
- 数值稠密每次需要对全部变量进行差分；本问题变量数为 `6×10+3×217=711`，残差评估成本随变量规模增长。
- 解析稀疏把每条观测只连接一个位姿和一个路标的结构显式提供给求解器，适合后续增加非关键帧、使用全部观测和扩展到更大场景。
- 当前解析雅可比并非完全解析：里程计因子仍使用局部有限差分；因此更准确的表述是**声呐重投影雅可比解析化 + 全局 BA 稀疏化**，不是"所有因子均纯解析"。

## 12. 改进 4：GNC + χ² 外点剔除

### 12.1 实现内容

基线使用固定 Huber `δ=20 px`。改进版采用两阶段鲁棒处理：

1. GNC 使用 Geman–McClure 型权重，从较宽松的近凸阶段开始；
2. 逐轮减小 `μ`，直至 `μ=1`，对大残差观测持续降权；
3. 用稳健尺度估计得到像素噪声尺度，并以 `χ²(2,0.99)=9.210` 做外点判定；
4. 将判定的外点权重置零，再用 Huber 做最终内点重优化；
5. GNC 内层复用改进 3 的解析稀疏雅可比。

### 12.2 可控外点实验

由于当前真实数据的原始测试中 Huber 外点数为 0，无法直接证明抗外点能力，因此在同一观测集上人工注入外点：随机选观测，在 `beam` 和 `range` 上分别加入 `40–80 px` 大偏移，扫描 15%、25%、35% 三种外点比例。位姿误差相对于无外点的干净参考解计算。

干净参考解重投影 RMS：约 **1.0145 px**；GNC 参数 `c=5 px`。

| 外点比例 | Huber 内点 RMS | GNC 内点 RMS | Huber 位姿误差 | GNC 位姿误差 |
|---|---:|---:|---:|---:|
| 15% | 4.968 px | **3.599 px** | 0.073 m | **0.015 m** |
| 25% | 8.049 px | **4.634 px** | 0.092 m | 0.189 m |
| 35% | 10.782 px | **2.755 px** | 0.166 m | **0.026 m** |

外点识别 F1（固定 Huber / GNC+χ²）分别为：

| 外点比例 | Huber F1 | GNC+χ² F1 |
|---|---:|---:|
| 15% | 0.99 | 0.83 |
| 25% | 0.97 | 0.83 |
| 35% | 0.96 | 0.83 |

### 12.3 结果解释

- **GNC 的主要收益是抗污染优化，不是单纯提高外点分类 F1。** 固定 Huber 通过残差阈值很容易检测本实验中人为注入的大偏移外点，但 Huber 的线性尾部仍会保留一定影响，导致位姿和内点拟合被拖偏。
- GNC-Geman–McClure 是冗降型鲁棒核，外层退火后大残差观测权重接近零，因此内点 RMS 在三档污染比例下都低于固定 Huber。
- 25% 外点时 GNC 的位姿误差为 0.189m，高于 Huber 的 0.092m，说明 GNC 对 `μ`、`c`、尺度估计和局部极小仍敏感；不能宣称在所有条件下全面优于 Huber。该档结果应作为论文中的失败/边界案例保留。
- 35% 外点时 GNC 的内点 RMS 约为 Huber 的四分之一，位姿误差也显著降低，说明在污染较重时冗降核的优势更明显。
- 当前外点是人工注入，不能替代真实错关联实验；真实数据还需要根据 track 关联错误、低 SNR、多径和阴影边界误检构造更物理的外点。

## 13. 效果图

`ba_improve34_gnc.png` 包含四个子图：

1. 内点重投影 RMS 随外点比例变化：GNC 曲线整体低于固定 Huber；
2. 位姿误差随外点比例变化：显示 GNC 的总体优势和 25% 边界案例；
3. 35% 外点时 GNC 的 `μ` 退火和平均权重变化；
4. 35% 外点时 GNC 的内点/外点残差分布。

## 14. 阶段性结论与后续工作

### 已验证

- 声呐重投影解析雅可比在数值上正确；
- 稀疏结构在当前规模上带来约 6 倍加速；
- GNC+χ² 在人工外点污染下显著降低内点重投影误差，尤其在 35% 高污染情况下；
- 改进 3 可作为改进 4 的高效内层求解器。

### 尚未完全解决

- 当前变量规模只有 711，规模扩大后应进一步使用 Schur 补和稀疏 Cholesky/Ceres/GTSAM；
- 里程计雅可比仍是局部有限差分；
- GNC 参数 `c`、退火速度、尺度估计需要在仿真和真实错关联数据上标定；
- χ² 判定可能误剔除部分高噪声内点，需采用观测协方差白化后再进行统计检验；
- 当前没有真实几何真值，外点实验主要证明鲁棒性趋势，不能直接等价于真实场景精度提升。

### 复现

```bash
python ba_improve34.py
```

输出：`BA自身改进34_测试结果.md`、`ba_improve34_gnc.png`，以及终端中的耗时、精度、外点比例扫描结果。

### 方法对应论文

- 解析雅可比、因子图、稀疏求解：Huang & Kaess ASfM、SIO、DISO。
- GNC：Yang & Carlone 的 graduated non-convexity 鲁棒估计思想。
- χ² 门控与数据关联：Huang & Kaess 增量数据关联、Westman 欠约束声呐 SLAM。


---

# 数据可用性分析（2026-09-02）

> 目的：盘点当前手上两类数据能否用于 BA 测试，明确"能测什么、不能测什么"，并给出补数据方向。

## 15. 现有真实数据集（项目自带）

**位置**：项目根目录 = `input/`（两处 MD5 完全相同，是同一份的副本）。
**文件**：`poses_est.npy`(10,4,4)、`pose_frame_ids.npy`(10)、`landmarks_final.npy`(217,3)、`tracks.csv`(14786 行)。

### 15.1 健康指标（自洽、真实）

| 项 | 数值 |
|---|---|
| 观测行数 / 不同帧 / 不同 track | 14786 / 60(0–59) / 386 |
| θ 范围 | ±0.501 rad（±28.7°）|
| ρ 范围 | 0.79–2.40 m |
| confidence | 均值 0.909，<0.5 仅 7.8% |
| 距离标定线性度 | `range=277.041·ρ−3.0`，残差 **0 px**（完美线性）|
| 方位标定线性度 | `beam=249.867·θ+127.5`，残差 std 0.67px、max 2.7px |
| 落在关键帧的观测 | 2449 / 14786（16.6%）|
| 每 track 被关键帧观测 | 均值 6.39 次，≥2 帧 359 个，≥3 帧 291 个 |
| 关联可恢复（暴力反推）| 200 / 217 |
| 初始重投影一致性 | RMS 5.29px，74% 在 5px 内，max 17.6px |

结论：数据**真实、自洽**，位姿/点云/观测三者互相吻合到几像素，BA 优化有意义——这也是改进 1–5 对照实验成立的基础。

### 15.2 两个硬伤（决定用途边界）

1. **运动近退化**：关键帧轨迹总长仅 **0.45 m**（平移跨度 dX=0.09/dY=0.38/dZ=0.17 m），路标平均距离 1.93 m，**基线/深度 ≈ 0.22**，偏航跨度仅 8°。→ 俯仰（Z）方向**本质不可观测**（λ₂/λ₃ 判据下 198 个多帧路标全部欠约束）。`landmarks_final` 里 Z 跨度 2.39m 本身就是不可靠三角化，不能当真值。
2. **无任何真值 + 利用率低**：无高度计/靶标/仿真真值；仅用 1641/14786（11%）观测；关联 200/217 有残余风险。

### 15.3 用途裁定

| 用途 | 可否 |
|---|---|
| 功能验证、跑通流程 | ✅ |
| 前后对照 / 方法 A vs B（相对提升）| ✅ |
| 重投影一致性、鲁棒性（注外点）| ✅ |
| **俯仰/Z 绝对精度** | ❌（不可观测 + 无真值）|
| **重建绝对精度 RMSE** | ❌（无真值）|

## 16. 外部数据集 `数据集（不上传git）/marine-debris-fls-datasets-master`

**是什么**：Marine Debris FLS Dataset（Valdenegro-Toro 等，OCEANS 2025；ARIS Explorer 3000；水池/转台/采石场）。四个子集：

| 子集 | 内容 | 原任务 |
|---|---|---|
| `quarry-fullsize` | 7209 张全幅声呐 PNG（按时间戳分文件夹）| 采石场巡检图像 |
| `turntable-cropped` | 4942 张裁剪图（声呐固定、物体转台旋转）| 分类 |
| `watertank-cropped` | 2364 张裁剪块 | 分类/匹配 |
| `watertank-segmentation` | 3737 图 + 1868 XML 框 + 掩膜（11 类+背景+Wall）| **语义分割** |

### 16.1 能否用于 BA：不能直接用

- **全库扫描确认无位姿/导航/tracks/点云/里程计**（`.npy/.csv/.mat/nav/pose/odom` 均为 0）；`loader.py` 只加载图像做分类。
- BA 吃的是前端产物（位姿 + tracks(θ,ρ) + 三角化路标 + 里程计），该集只有**原始图像 + 分类/分割标注**，缺整条图像→几何前端链路。
- 转台子集**声呐固定、无载体运动**，非 SfM 场景；采石场虽像运动序列但**只有图像、无导航位姿**。
- 既无位姿真值也无 3D 真值 → 无法做绝对精度验证。

### 16.2 但对本项目另有价值

1. **创新点二（阴影→仰角先验）的真实开发数据 ★**：`watertank-segmentation` 有真实声呐图 + 逐像素掩膜。目标区域已知后可推导其后方声学阴影，是把创新点二从"接口"推进到"真实数据"最现实的素材。
2. **前端图像理解验证**：真实 FLS 图可测 CFAR/A-KAZE 检测、目标背景分割（对应大论文"图像理解层"）。

## 17. 结论与补数据方向

- **BA 绝对精度测试目前无合适数据**：自带集运动近退化 + 无真值；外部集无位姿/几何真值。
- **相对提升与功能验证已充分**：改进 1–5 的对照结论均基于自带集成立。
- **补数据两条路**：
  1. **仿真数据（最快）**：已知场景 + 运动多样性足够的轨迹 → 正演 θ/ρ 观测，天然带真值点云、`landmark_id`、真实里程计、已知仰角孔径。可把改进 1/2/4/5 从"相对提升"升级为"对真值的绝对 RMSE"，并提前验证"何种运动使俯仰可观测"。
  2. **实采序列**：带导航位姿（动捕/USBL/DVL/IMU）+ 靶标真值 + 更大平移基线 + 升沉/俯仰运动（破除退化），详见 `BA代码/上游对接清单.md` 与采集要点。
- **创新点二**：可先用 `watertank-segmentation` 真实图开发"目标/阴影分割 → 高度反演 → 仰角先验"。
