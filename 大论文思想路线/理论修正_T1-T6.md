# 理论修正 T1–T6（权威依据）

> 本文是**公式层的唯一权威**。`创新点内容设计_定稿.md`、`稠密化模块_点云生成内容设计.md`、`实施任务表_验收标准_阶段安排.md`、`文献分析与思路重构_v3.md` 中与本文冲突的公式**一律以本文为准**。
> 触发：2026-09-04 的理论自查发现 5 处实质错误 + 1 处理论补全。
> 版本：2026-09-04

---

## 0. 六条修正一览

| # | 类型 | 原表述 | 修正后 | 影响范围 |
|---|---|---|---|---|
| **T1** | ❌ **用错物理量** | $\varphi_{\text{blind}}=\arcsin(\delta\rho/t_z)$ | CRLB 形式 $\varphi_{\text{blind}}=\arcsin\dfrac{\sigma_\rho}{\sqrt{N}\,\tau_z}$ | ★I-1 的整个推导 |
| **T2** | ❌ **阈值不可达** | $\kappa_j=\lambda_3/\lambda_2>0.05$ | 阈值由精度要求经 CRLB 反推；并声明 0.05 对 ARIS 几何**数学上不可达** | `observability.py`、三分策略、T1.2/T2.3 验收 |
| **T3** | ⚠️ **约束不完整** | 只有量程约束 $h\le z_s(1-d/D_{\max})$ | 补**孔径跨度约束**（真实 ARIS 下它先 binding） | ★I-2 包线、构型设计、S5/S6 场景 |
| **T4** | ⚠️ **丧失保证未声明** | 置信度加权雕刻 | 声明丧失 Aykin 的 $S\subseteq\tilde S$ 包含性保证，改给概率版本 | ★II-2 的表述与验收 |
| **T5** | ⚠️ **公式缺项** | $\hat e_j=\arcsin\big((z_{\text{top}}-z_b)/r_j\big)$ | 补基准帧旋转 $R_b$ | 创新二步骤 4 |
| **T6** | ➕ **理论补全** | "heave 越大越好"（经验错误） | $A\lesssim d\tan\varphi_{\max}$，存在最优幅度 | 构型设计、T1.2 |

---

## 1. T1：仰角盲区角必须用 CRLB，而非图像位移可检测性

### 1.1 原错误

原式 $\varphi_{\text{blind}}=\arcsin(\delta\rho/t_z)$ 来自 Wang 2023（arXiv:2307.16160）的 heave 位移式 $|\mathrm{d}| = t_z|\sin\varphi|$，判据是"位移超过一个距离分辨单元才可检测"。

**错在**：Wang 2023 的语境是**自监督视图合成**——他们需要帧间位移在图像上肉眼/网络可辨。而本文是 **BA 的可观测性**，属 Fisher 信息问题，与"单帧位移是否可见"不是同一件事。用前者当后者会得到偏乐观的预测（实测：$h=0.4$ m 时原式预测约 94% 可观测，真实良约束为 **0**）。

### 1.2 正确推导

前视声呐单次观测给出 $(\theta,\rho)$。对地标世界坐标 $P=(P_x,P_y,P_z)$：

$$\theta=\operatorname{atan2}(P^b_y,P^b_x),\qquad \rho=\|P^b\|,\qquad P^b=R_k^\top(P-t_k)$$

关键事实：**方位角对垂直坐标不敏感**。$\partial\theta/\partial P^b = \dfrac{(-P^b_y,\;P^b_x,\;0)}{(P^b_x)^2+(P^b_y)^2}$ 的第三分量恒为 0，故 $\theta$ 不携带任何 $P_z$ 信息。$P_z$ 的信息**只**来自斜距：

$$\frac{\partial\rho}{\partial P^b_z}=\frac{P^b_z}{\rho}=\sin\varphi$$

于是单次观测对垂直方向的 Fisher 信息为 $\sin^2\varphi_k/\sigma_\rho^2$，$N$ 次观测累加后的 CRLB：

$$\boxed{\;\sigma_{P_z}\;\ge\;\frac{\sigma_\rho}{\sqrt{\sum_{k=1}^{N}\sin^2\varphi_k}}\;\xrightarrow{\ \varphi_k\approx\varphi\ }\;\frac{\sigma_\rho}{\sqrt{N}\,|\sin\varphi|}\;}$$

要求高度精度达 $\tau_z$，则可观测的最小仰角（**盲区角**）为

$$\boxed{\;\varphi_{\text{blind}}=\arcsin\frac{\sigma_\rho}{\sqrt{N}\,\tau_z}\;}$$

### 1.3 数值（ARIS Explorer 3000）

$\sigma_\rho=10$ mm（规格 3–19 mm 取中）：

| $N$ | $\tau_z=2$ cm | $\tau_z=5$ cm | $\tau_z=10$ cm |
|---|---|---|---|
| 5 | 12.9° | 5.1° | 2.6° |
| 10 | 9.1° | 3.6° | 1.8° |
| 20 | 6.4° | 2.6° | 1.3° |
| 50 | 4.1° | 1.6° | 0.8° |

**与孔径对照**：ARIS 垂直孔径仅 $\pm7.5°$。要求 $\tau_z=2$ cm 时，$N=10$ 需 $|\varphi|>9.1°$——**超出孔径，任何地标都达不到**。$\tau_z=5$ cm、$N=10$ 时 $\varphi_{\text{blind}}=3.6°$，占孔径的 **48%**——近半个孔径是盲区。

### 1.4 为什么这一版更强

1. 是 **CRLB**（有下界含义），不是启发式；
2. 显式含**精度要求 $\tau_z$ 与观测数 $N$**，与工程需求直接挂钩；
3. 直接对应代码里已算的 $\lambda_3$（见 T2），不需另立体系；
4. 解释了实测——原式解释不了 $h=0.4$ 时良约束为 0，新式可以（盲区占近半孔径 + 地标 $\varphi$ 分布集中在孔径中心）。

### 1.5 论文表述

原"运动幅度越大盲区越小"的叙事作废。新叙事：**$P_z$ 的信息只来自斜距且正比 $\sin^2\varphi$，因此靠近仰角孔径中心的目标点，其高度在给定精度要求下不可观测，且该结论与运动方式无关——运动只能改变 $\varphi_k$ 的分布，不能突破孔径 $\varphi_{\max}$ 的硬上界。**

---

## 2. T2：$\lambda_3/\lambda_2>0.05$ 对真实 ARIS 几何数学上不可达

### 2.1 判据的几何含义

`observability.py` 构建 $\Lambda_j=\sum_k J_k^\top J_k$，$J_k=\begin{bmatrix}A\,\partial\theta/\partial P\\ C\,\partial\rho/\partial P\end{bmatrix}$。

单次观测时，仰角方向 $e_\varphi$ 严格落在零空间：$\partial\theta/\partial P\cdot e_\varphi=0$（方位梯度纯水平且无仰角分量），$\partial\rho/\partial P\cdot e_\varphi=0$（斜距梯度沿视线，$e_\varphi$ 垂直于视线）。故 $\lambda_3=0$，**必须 $\ge2$ 次不同姿态的观测**。

多次观测下，$\lambda_3$ 的量级由观测集内**视线仰角的变化幅度** $\Delta\varphi_{\text{eff}}$ 决定：

$$\frac{\lambda_3}{\lambda_2}\;\sim\;\Delta\varphi_{\text{eff}}^2$$

故 $\lambda_3/\lambda_2>0.05\;\Longleftrightarrow\;\Delta\varphi_{\text{eff}}>\arcsin\sqrt{0.05}=12.9°$。

### 2.2 与孔径的冲突

| 传感器 | 垂直孔径 | 判据需要 $\Delta\varphi$ | 可达性 |
|---|---|---|---|
| ARIS Explorer 3000（真实） | 15°（$\pm7.5°$） | 12.9° | ⚠️ 需扫过孔径 **86%**，实际几乎不可能 |
| ARIS 单帧 $\sin^2\varphi$ 上界 | $\sin^2 7.5°=0.017$ | $>0.05$ | ❌ **数学上不可达，`well_mask` 恒为假** |
| 仿真 `config.py`（$\pm17°$） | 34° | 12.9° | ✅ 可达（但与真实不一致） |

**结论**：0.05 不只是"拍的数"，对真实 ARIS 是**几何上不可能满足的**。这解释了修 bug 后真实良约束仅 0–7 个。

### 2.3 修正后的判据

**弃用魔数，改由 T1 的 CRLB 反推**，并显式分四类（不再二分）：

$$\text{类别}(j)=\begin{cases}
\textbf{观测不足} & N_j<2\ \text{或}\ \operatorname{tr}(\Lambda_j)=0\\[2pt]
\textbf{盲区} & \hat\sigma_{P_z,j}>\tau_z\quad\big(\text{等价 }|\varphi_j|<\varphi_{\text{blind}}\big)\\[2pt]
\textbf{弱约束} & \tau_z\ge\hat\sigma_{P_z,j}>\tau_z/2\\[2pt]
\textbf{良约束} & \hat\sigma_{P_z,j}\le\tau_z/2
\end{cases}$$

其中 $\hat\sigma_{P_z,j}=\sqrt{\big[\Lambda_j^{-1}\big]_{zz}}$，$\Lambda_j$ 已按 $\Sigma^{-1}$ 白化并按置信度 $w$ 加权（伪逆处理奇异情形）。

**这个判据有量纲（米）、有工程含义（高度精度）、与孔径无冲突，且对任何传感器通用。**

### 2.4 必须同时修的实现缺陷

原代码 `l3=np.maximum(ev[:,2],eps); l2=np.maximum(ev[:,1],eps); well=(l3/l2)>0.05`，在 $\Lambda_j=0$（观测数<2）时得 $10^{-9}/10^{-9}=1>0.05$ ⇒ **零信息地标被误判为良约束**。实测影响：

| 配置 | well 报告 | 其中误判 | 真实良约束 | 观测数<2 |
|---|---|---|---|---|
| general h=0.4 | 1/30 | 1 | **0** | 1 |
| general h=0.8 | 9/30 | 2 | **7** | 2 |
| general h=1.2 | 24/30 | **21** | **3** | **21** |
| forward 全档 | 2/30 | 2 | **0** | 2 |

**先前所有基于 well 比例的结论均需按此表重算。** 特别是"29/30 欠约束"应为"**30/30 全部欠约束**"。

---

## 3. T3：可反演性包线必须补孔径跨度约束

### 3.1 完整的三约束（平坦海底 + 直立目标）

记声呐高度 $z_s$、目标高 $h$、目标底地面距离 $D_t$、阴影末端地面距离 $D_e$、量程 $\rho_{\max}$、垂直半孔径 $\varphi_{\max}$、下俯角 $\theta_p$。

由掠顶射线几何：$D_e = \dfrac{D_t\,z_s}{z_s-h}$，反演式 $h=z_s\Big(1-\dfrac{D_t}{D_e}\Big)$。

| # | 约束 | 表达式 | 物理含义 |
|---|---|---|---|
| **C-I** | 高度上界 | $h<z_s$ | 声呐必须高于目标顶，否则掠顶射线向上、永不落地、阴影无限长 |
| **C-II** | 量程 | $h\le z_s\Big(1-\dfrac{D_t}{D_{\max}}\Big),\quad D_{\max}=\sqrt{\rho_{\max}^2-z_s^2}$ | 阴影末端必须落在量程内 |
| **C-III** | **孔径跨度（新增）** | $\dfrac{h\,D_t}{D_t^2+z_s(z_s-h)}\le\tan(2\varphi_{\max})$ | 目标底与阴影末端的俯角跨度必须同时落在垂直孔径内 |
| C-IV | 下俯角 | $\theta_p\in\Big[\arctan\dfrac{z_s}{D_t}-\varphi_{\max},\ \arctan\dfrac{z_s}{D_e}+\varphi_{\max}\Big]$ | 该区间非空是 C-III 的等价条件；给出 $\theta_p$ 的可行范围 |

**C-III 推导**：跨度 $=\arctan\frac{z_s}{D_t}-\arctan\frac{z_s-h}{D_t}\le 2\varphi_{\max}$。用 $\arctan u-\arctan v=\arctan\frac{u-v}{1+uv}$，代入 $u=\frac{z_s}{D_t}$、$v=\frac{z_s-h}{D_t}$ 得 $\frac{h/D_t}{1+z_s(z_s-h)/D_t^2}\le\tan(2\varphi_{\max})$，整理即得。

### 3.2 关键结论：真实 ARIS 下 C-III 先 binding

验算（ARIS $\varphi_{\max}=7.5°$、$\rho_{\max}=15$ m、$z_s=4$ m、$D_t=6$ m）：

| $h$ | C-II 上限 | C-III 跨度 | C-III 是否满足（$\le15°$） | 判定 |
|---|---|---|---|---|
| 0.5 m | 2.34 m ✓ | 3.44° | ✓ | 可反演，余量充裕 |
| 1.5 m | 2.34 m ✓ | 11.07° | ✓ | 可反演 |
| **2.0 m** | **2.34 m ✓** | **15.26°** | **✗** | **不可反演——量程够但孔径不够** |

**所以先前只写量程约束是不完整的，而且在真实 ARIS 参数下会给出错误的"可反演"判定。** 论文应写清"哪个约束在哪个参数区间 binding"——这比只给一个上限更有价值，也是 ★I-2 的实质内容。

---

## 4. T4：加权雕刻丧失包含性保证，必须声明

### 4.1 基线的保证

Aykin 2017 的硬雕刻是**集合运算**：只要某视角判定体素为"空"就删除。其理论保证是

$$S\subseteq\tilde S\subseteq H_\alpha\subseteq H_C$$

即估计壳**必然包含**真实目标（$S$ 为真形状，$\tilde S$ 为估计，$H_\alpha$/$H_C$ 为 $\alpha$-壳/凸壳）。这是该方法的核心理论卖点。

### 4.2 本文的代价与替代

改为对数几率软累加 $\ell(v)\leftarrow\ell(v)+\sum_k\bar c_k s_k(v)$ 后，**单次可靠的"空"表态不再必然导致删除**，故 $S\subseteq\tilde S$ **不再成立**。

**必须在论文中显式声明这一取舍**，并给出概率替代：

$$\Pr\big[S\subseteq\tilde S_\alpha\big]\ge 1-\alpha,\qquad \tilde S_\alpha=\{v:\ p(v)\ge\alpha\}$$

即以阈值 $\alpha$ 提取的壳以 $1-\alpha$ 的置信度包含真实目标。**验收方式**：在仿真（有 GT）上实测"真实表面点落在 $\tilde S_\alpha$ 内的比例"，检验其与 $1-\alpha$ 的一致性（校准性）。

### 4.3 表述纪律

- 不得声称"本文方法保持了 Aykin 的包含性保证"；
- 应表述为"以确定性包含保证换取对边缘定位误差的鲁棒性，并给出概率形式的包含性并实测其校准性"；
- 若校准性实测不成立（覆盖率显著低于 $1-\alpha$），★II-2 的表述需退回"经验改进"，不得主张概率保证。

---

## 5. T5：$h\to\hat e_j$ 转换必须含基准帧旋转

### 5.1 原错误

原式 $\hat e_j=\arcsin\dfrac{z_{\text{top}}-z_b}{r_j}$ 隐含假设基准帧 $R_b=I$（无俯仰无横滚）。而本文构型明确要求**固定下俯 15–20°**，$R_b\ne I$，原式失效。

### 5.2 正确式

地标在基准帧 $b$ 下的仰角定义为 $e_j=\arcsin\big(P^{b}_{j,z}/r_j\big)$，$P^b_j=R_b^\top(P_j-t_b)$。以阴影反演的目标顶点 $P^w_{\text{top}}$（世界坐标，其 $z$ 分量为 $z_{\text{top}}=z_{\text{floor}}+h$，水平位置由该目标的方位与距离给出）代入：

$$\boxed{\;\hat e_j=\arcsin\frac{\big[R_b^\top\big(P^w_{\text{top}}-t_b\big)\big]_z}{r_j}\;}$$

不确定度由链式法则传播（$R_b$ 视为已知）：

$$\sigma_{e,j}=\frac{\big|\big(R_b^\top\hat z\big)_z\big|\cdot\sigma_{z_{\text{top}}}}{r_j\cos\hat e_j},\qquad \sigma_{z_{\text{top}}}^2=\sigma_h^2+\sigma_{\text{floor}}^2$$

其中 $\hat z$ 为世界竖直单位向量。**当 $R_b=I$ 时退化为原式**，可作为单元测试的一致性检查。

有效性条件：$\big|\big[R_b^\top(P^w_{\text{top}}-t_b)\big]_z\big|<r_j$，否则该先验作废。

### 5.3 $\sigma_h$ 的完整传播（补全）

由 $h=z_s(1-D_t/D_e)$、$D=\sqrt{\rho^2-z_s^2}$：

$$\frac{\partial h}{\partial D_e}=\frac{z_s D_t}{D_e^2},\quad \frac{\partial h}{\partial D_t}=-\frac{z_s}{D_e},\quad \frac{\partial h}{\partial z_s}=\frac{h}{z_s}+z_s\frac{\partial}{\partial z_s}\Big(\!-\frac{D_t}{D_e}\Big)$$

$$\frac{\partial D}{\partial\rho}=\frac{\rho}{D},\qquad \frac{\partial D}{\partial z_s}=-\frac{z_s}{D}$$

**先前的实现只传播了 $\sigma_L$ 与 $\sigma_e$，遗漏了 $\sigma_{z_s}$（声呐高度不确定度）**。高度由 DVL/深度计给出，其误差不可忽略，必须纳入——否则 $\sigma_h$ 系统性偏小，会导致门控权重偏大（过度信任阴影先验）。

---

## 6. T6：heave 最优幅度的理论解释（补全，非纠错）

### 6.1 两个相反的效应

- **有利**：增大 heave 幅度 $A$ 扩大 $\varphi_k$ 的分布范围，提升 $\sum\sin^2\varphi_k$ 与 $\Delta\varphi_{\text{eff}}$，改善可观测性；
- **不利**：目标保持在垂直孔径内的条件为 $|\Delta z|\lesssim D_t\tan\varphi_{\max}$，$A$ 过大则目标频繁跑出孔径，观测数骤降至 $<2$，$\Lambda_j$ 退化为零矩阵。

### 6.2 最优幅度

$$\boxed{\;A_{\text{opt}}\approx D_t\tan\varphi_{\max}\;}$$

**预测与实测对照**（$D_t\approx3$ m、仿真 $\varphi_{\max}=17°$）：$A_{\text{opt}}\approx3\times0.306=0.92$ m。

| $A$ | 预测 | 实测真实良约束 | 实测观测数<2 |
|---|---|---|---|
| 0.4 m | 偏小，$\Delta\varphi$ 不足 | 0 | 1 |
| 0.8 m | 接近最优 | **7（最佳）** | 2 |
| 1.2 m | 超出，目标出孔径 | 3 | **21** |

**预测与实测吻合**：最优值在 0.8–0.9 m 之间，1.2 m 时崩溃。

### 6.3 由此得到的构型结论（论文可直接写）

1. **平移类激励（heave）自带上界**，因为它把目标推出孔径；
2. **roll 不受此限**——它绕视线轴旋转，改变歧义方向而不改变目标在孔径中的位置，因此在**同等激励幅度下可获得更大的 $\Delta\varphi_{\text{eff}}$**。这从理论上解释了 Aykin 2017 §V 的经验结论（roll 优于环绕/平移），也与 Wang 2023 的敏感度式（roll 项含 $r$ 因子）一致；
3. 实际 AUV 的 roll 能力受限（Aykin 用固定架人为 roll，Feng 2024 用铝管 + 水下动捕），这一工程约束应正面讨论，并给出"受限 roll + 最优 heave"的组合方案。

---

## 7. 修正带来的定级与叙事变化

| 项 | 变化 |
|---|---|
| **★I-1** | 保留且**增强**。主张从"给出盲区角"升级为"**给出 FLS 高度可观测性的 CRLB 与孔径硬上界，证明存在与运动方式无关的不可观测区**"。原公式作废 |
| **★I-2** | 保留且**增强**。补 C-III 后，主张包含"**哪个约束在哪个参数区间 binding**"，比单一上限更有内容；且揭示真实 ARIS 下孔径先 binding |
| **★II-1** | 不变，但 $\sigma_h$ 须补 $\sigma_{z_s}$ 项，否则门控过度信任先验 |
| **★II-2** | 表述收紧：明确声明丧失确定性包含保证，改主张概率包含并实测校准性 |
| **★II-3** | 不变 |
| **新增叙事（重要）** | $\varphi\approx0$ 的地标高度信息 $\propto\sin^2\varphi\to0$，而阴影只给"目标顶高"这一**上界**。故这类点的信息量只支持"**给出边界**"而非"**测出数值**"。**空间雕刻输出包围壳，恰是与信息量匹配的估计器**——这比"跟随文献"有力得多，应写入方法章作为路线选择依据，并据此把 Z 向精度主张限定为"有界"而非"精确" |

---

## 8. 两项基准决策（已定，2026-09-04）

### 8.1 垂直孔径口径：**方案 C（主 A + B 作敏感性档）** ✅ 已定

| 档 | 垂直半孔径 | 角色 | 用途 |
|---|---|---|---|
| **主档** | $\varphi_{\max}=7.5°$（ARIS Explorer 3000 真实值，全宽 15°） | **全部主结论** | 与 `watertank-segmentation` / `quarry-fullsize` / `turntable-cropped` 三个真实子集同构，仿真结论可外推到真实数据 |
| 敏感性档 | $\varphi_{\max}=17°$（现 `config.py` 值） | 孔径敏感性分析 | 定量刻画"孔径是硬上界"这一 ★I-1 结论：同一场景仅改孔径，观察盲区占比与可反演域的变化 |

**决策理由**：主档取真实值才能与真实数据互推（否则 R3–R5 的真实验证与仿真结论无法对话）；同时保留宽孔径档，把孔径本身变成一个**受控变量**——这正好把 ★I-1 的核心主张（孔径是无法用运动突破的硬上界）从定性论断变成一条可画出来的曲线。**孔径从"参数设定"升级为"实验证据"，这是方案 C 相对 A 或 B 的额外收益。**

**实现要求**：`config.py` 提供两组预设 `SONAR_ARIS`（$\pm7.5°$、30° 方位、128 波束）与 `SONAR_WIDE`（$\pm17°$、130° 方位、512 波束）；`feasibility.py` 与所有实验脚本接受孔径为显式入参，禁止硬编码。

### 8.2 高度精度基准 $\tau_z$：**主用 5 cm，并作为报告轴扫 {2, 5, 10} cm** ✅ 已定

$\tau_z$ 是修正后**唯一需要人为设定的量**，四分类与盲区角都由它定。取值与依据：

| 依据 | 内容 |
|---|---|
| **横向分辨率同量级** | Aykin 2017 的可分辨距离 $d_R=R\,d\theta$：ARIS $d\theta=0.25°$、$\rho=10$ m ⇒ $d_R=4.4$ cm。**垂直精度要求取与横向分辨率同量级是自然的**——要求垂直精度显著优于横向分辨率没有工程意义 |
| **相对精度合理** | 目标高度量级 0.5–2.5 m ⇒ 5 cm 对应 2%–10% 相对精度 |
| **物理可达** | ARIS 距离分辨率 3–19 mm，5 cm 相当于 2.6–17 个分辨单元，可达 |
| **文献量级参照** | Feng 2024 DSC 在水池（0.5×0.1×0.6 m 物体）达 HD = 2.25 cm，优于 5 cm ⇒ 5 cm 是保守且可达的基准 |
| **实验可分辨性（关键）** | 见下表——5 cm 使孔径被近乎均分为盲区/可观测两半，实验对比度最大 |

**$\tau_z$ 对盲区占比的影响**（ARIS 主档：$\sigma_\rho=10$ mm、$N=10$、$\varphi_{\max}=7.5°$）：

| $\tau_z$ | $\varphi_{\text{blind}}$ | 盲区占孔径 | 实验价值 |
|---|---|---|---|
| 2 cm | 9.1° | **100%**（超出孔径） | 极端档：证明"存在精度要求下全孔径不可观测"，但无对比度 |
| **5 cm** | **3.6°** | **48%** | **主档：近乎均分，盲区/可观测两类样本量均衡，判据的判别力最强** |
| 10 cm | 1.8° | 24% | 宽松档：多数可观测，用于展示判据在另一端仍成立 |

**把 $\tau_z$ 做成报告轴而非常数，是消除"魔数质疑"的根本办法**：论文不声称"$\tau_z$ 应该是 5 cm"，而是给出**盲区占比随 $\tau_z$ 的曲线**，并指出该曲线与孔径 $\varphi_{\max}$ 的交点（即 $\tau_z^{\text{crit}}=\sigma_\rho/(\sqrt N\sin\varphi_{\max})$，ARIS 主档下为 **2.4 cm**）——**低于此精度要求时全孔径不可观测**。这个临界精度是一个干净的、可直接引用的传感器能力指标。

$$\boxed{\;\tau_z^{\text{crit}}=\frac{\sigma_\rho}{\sqrt N\,\sin\varphi_{\max}}\quad\text{（ARIS, }N=10\text{: }2.4\text{ cm）}\;}$$

**这个临界值本身是 ★I-1 的一个附带产物，建议单列为一个小结论。**
