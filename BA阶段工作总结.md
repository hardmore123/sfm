# BA 阶段工作总结与效果

> 汇总声呐三维重建 BA（光束平差）阶段**已完成的工作、量化效果、产出物与后续方向**。
> 数据规模：关键帧 10、初始路标 217、参与 BA 观测 1641（原始观测 14786）。
> 环境：Python 3.14、numpy 2.5.1、scipy 1.18.0。
> 日期：2026-08-28

---

## 一、总体进度一览

| 板块 | 状态 | 产出 |
|---|---|---|
| BA 基线（跑通真实数据） | ✅ 完成 | `ba_optimize.py`，重投影 RMS 5.29→1.01px |
| 算法改进 1&2（参数化 + 欠约束建模） | ✅ 完成并测试 | `ba_improve.py` + 报告 + 3 图 |
| 算法改进 3&4（解析稀疏 + 鲁棒） | ✅ 完成并测试 | `ba_improve34.py` + 报告 + 1 图 |
| 上游对接规范 | ✅ 完成 | `上游对接清单.md`（字段/示例/验收/两条路径）|
| 代码版本归档 | ✅ 完成 | `BA代码/`（6 代码 + README + 对接文档）|
| 实验日志与专家分析 | ✅ 完成 | `BA改进实验日志.md` |
| GitHub 备份 | ✅ 完成 | https://github.com/hardmore123/sfm |
| 改进 5–9（视场约束/阴影先验等） | ⏳ 待做 | 见后续方向 |
| 上游真实字段接入 | ⏳ 待上游 | landmark_id / 标定 / 协方差 |

---

## 二、BA 基线：从"能跑"到有据可查

### 做了什么

用 `scipy.least_squares(trf)` 对声呐 SfM 结果做联合优化，同时估计 **10 个关键帧位姿（6 维）+ 217 个路标（3 维）**，四类残差：

1. 首帧先验（消规范自由度，w=1000）
2. 里程计相对位姿（SE(3) 相对约束，w=100）
3. 声呐重投影（像素量纲 `beam=A·θ+B`、`range=C·ρ+D`，w=1）
4. 路标弱先验（拉向初值稳定欠约束点，w=1）

固定 Huber 鲁棒核 δ=20px。

### 效果

- 重投影 RMS：**5.29 px → 1.01 px**。
- 建立了完整的数据加载、track→landmark 关联、像素自标定、里程计推导、可视化与报告流程，成为后续所有改进的公共基座。

### 已识别的三处临时补丁（后续对接重点）

| 补丁 | 现状 | 风险 |
|---|---|---|
| track→landmark 关联 | 暴力重投影反推 | 仅 200/217，可能配错 |
| 观测权重 | 统一权重 1 | 无物理依据、量纲不一致（rad vs m）|
| 里程计约束 | 从位姿差分反推、权重猜 100 | 非真实里程计精度 |

---

## 三、算法改进 1&2：几何可信性（`ba_improve.py`）

### 问题根源

前视声呐单帧只测方位 θ 与斜距 ρ，**俯仰角不可观测**——同一像素对应一整条"俯仰弧"。基线用世界笛卡尔 (x,y,z) + 全 3-DOF + 弱先验，导致欠约束路标的 Z 沿俯仰弧自由滑动去"迎合"观测（即 Westman 2018 所述"强行三角化欠约束路标污染估计"）。

### 做了什么

- **改进 1（相对球坐标）**：路标改为相对基准帧的 (ψ, r, elev)，把不可观测的俯仰自由度显式隔离成独立参数 elev。
- **改进 2（欠约束分类）**：对每个路标用观测构建单点 `JᵀJ`(3×3)，按 **λ₂/λ₃ < 20** 判定良/欠约束；欠约束者把 elev 移出优化、在物理仰角孔径内网格搜索并交替再拟合（坐标下降 4 轮）。
- 不改动基线，`import ba_optimize` 复用其数据流，保证三方对照口径一致。

### 效果（前后对照）

| 指标 | 优化前 | 基线 BA | 改进 BA | 变化 |
|---|---|---|---|---|
| 重投影 RMS (px) | 5.29 | 1.01 | 1.21 | ↑约0.2px（合理代价）|
| 方位 θ RMS (rad) | 0.0198 | 0.00383 | 0.00406 | 基本持平 |
| 斜距 ρ RMS (m) | 0.0061 | 0.0027 | 0.0034 | 略升 |
| 路标总位移 mean (m) | — | 0.277 | 0.091 | **↓67.3%** |
| **路标 Z 位移 mean (m)** | — | 0.269 | 0.082 | **↓69.5%** |
| **路标 Z 位移 max (m)** | — | 1.928 | 1.409 | ↓26.9% |

### 核心结论

1. **抑制俯仰乱跑是主要收益**：Z 向漂移 ↓70%，点云几何更可信。重投影 RMS 略升 0.2px 不是退步——基线那点优势正来自"不受约束地在 Z 方向乱移路标"，改进版禁止了这种"用不可信自由度换低残差"。
2. **一个有论文价值的发现**：判据下 **198 个多帧路标全部为欠约束**（λ₂/λ₃ 最小 25.69）。这量化证明当前轨迹（前向平移 + 小幅偏航）落在退化运动区间，**俯仰几何本质不可观测**——为"必须引入阴影先验补俯仰"提供实证支点。

### 产出

`landmarks_improved.npy/.ply`、`BA自身改进_测试结果.md`、`ba_improve_cloud.png`、`ba_improve_zdrift.png`、`ba_improve_metrics.png`。

---

## 四、算法改进 3&4：效率与鲁棒（`ba_improve34.py`）

### 改进 3：解析雅可比 + 稀疏求解

- 推导声呐重投影对位姿/路标的解析导数（关键简化：`P_b=Rᵀ(P−t)` 使平移雅可比 = −路标雅可比；旋转用欧拉角解析导数）；按 BA 二部图结构向量化填充稀疏 CSR。
- 经有限差分校验，最大绝对误差 ≈ 3.7e-4。

| Jacobian 模式 | 用时 | 最终 cost | 重投影 RMS |
|---|---|---|---|
| 数值稠密（基线） | 73.34 s | 994.929 | 1.0137 px |
| 数值 + 稀疏 | 22.21 s | 994.956 | 1.0145 px |
| **解析 + 稀疏** | **12.28 s** | 994.956 | 1.0145 px |

**效果：≈6× 加速，解完全一致**（cost/RMS 不变）——证明解析雅可比正确、稀疏化只提速不损精度。

### 改进 4：GNC + χ² 外点剔除

- GNC（Geman–McClure）μ 从大退火到 1，外层 IRLS 重加权，内层复用改进 3 的解析稀疏雅可比；χ²(2,0.99)=9.21 门限判外点后再用 Huber 重优化。
- 可控实验：在 beam/range 各注入 40–80px 大偏移，扫描 15/25/35% 外点比例。

| 外点比例 | Huber 内点 RMS | GNC 内点 RMS |
|---|---|---|
| 15% | 4.97 px | **3.60 px** |
| 25% | 8.05 px | **4.63 px** |
| 35% | 10.78 px | **2.76 px** |

**效果：GNC 内点 RMS 各比例下一致优于固定 Huber，优势随污染加剧扩大（35% 时约 3.9×）**。原因：Huber 的二次-线性核对大外点仍保留线性尾部影响；GNC-GM 是冗降核，收敛后外点权重趋 0。

**诚实标注**：25% 档 GNC 位姿误差（0.189m）高于 Huber（0.092m），说明 GNC 对 μ/c/尺度估计与局部极小仍敏感，不宜宣称全面占优——作为边界案例保留。

### 产出

`BA自身改进34_测试结果.md`、`ba_improve34_gnc.png`（4 子图）。

---

## 五、上游对接工作

### 量化了缺失关联的损失（最有说服力的对接理由）

```
全部观测 14786 条
  ↓ 只有 10 个关键帧位姿 → 非关键帧观测丢弃
落在关键帧 2449 条 (16.6%)
  ↓ track→landmark 需 BA 反推，仅 200/217 成功
BA 实际使用 1641 条 (11.1%)   ← 近 89% 被浪费
```

### 需要上游提供（按优先级）

| 优先级 | 项目 | 收益 |
|---|---|---|
| P0 | `landmark_id` 一列 | 消除关联错误，删掉暴力反推 |
| P0 | `sensor_calib.yaml`（映射/视场/仰角孔径/外参/单位/欧拉序）| 防方向/单位错误 |
| P1 | `sigma_theta` / `sigma_rho` | 残差白化，解更准更抗差 |
| P1 | `odom_rel.csv`（真实相对位姿+协方差）| 权重有物理依据 |
| P2 | 非关键帧位姿/增量 | 可用观测 1641→约 14786（≈9×）|

对接文档给出**两条路径**（A 落盘 5 文件、B 内存传参契约）、**BA 侧 6 个具体改动位置**，以及**验收清单**（含"用标定映射正算 θ/ρ→beam/range 残差应 <1px"这类抓语义错的检查）。详见 `BA代码/上游对接清单.md`。

---

## 六、代码版本归档

`BA代码/` 目录汇总全部 BA 版本：

| 版本 | 文件 | 定位 |
|---|---|---|
| V1 | `12.5新.py` | 前端原型，相对球坐标思路起源 |
| V2 | `ba_optimize.py` | 项目基线（冻结） |
| V3 | `ba_patent.py` | 专利开发版（当前=基线副本+独立输出）|
| **V4** | **`ba_improve.py`** | **★最新：改进 1&2（几何可信）** |
| **V5** | **`ba_improve34.py`** | **★最新：改进 3&4（效率鲁棒）** |
| 工具 | `view3d.py` | 交互式 3D 查看器 |

> V4/V5 是两条正交分支（一个动参数化、一个动求解器与鲁棒核），尚未合并。附 `README.md`（逐版详解+演进图+运行说明）与 `上游对接清单.md`。

---

## 七、量化效果汇总（一张表）

| 维度 | 基线 | 改进后 | 提升 |
|---|---|---|---|
| 重投影 RMS | 5.29→1.01 px | 1.21 px（几何可信）| 数据拟合到像素级 |
| 路标 Z 向漂移 mean | 0.269 m | 0.082 m | **↓69.5%** |
| 求解速度（解析稀疏） | 73.34 s | 12.28 s | **≈6×** |
| 高外点(35%)内点 RMS | 10.78 px | 2.76 px | **≈3.9×** |
| 数据利用率 | 11%（1641/14786）| 待上游补位姿后 →≈100% | 潜在 ≈9× |

---

## 八、局限与后续方向

### 当前局限（诚实说明）

1. **缺真值**：目前只能用重投影一致性与位移稳定性侧证，无 Z 方向绝对精度真值。
2. **仰角孔径为假设值**（±0.30rad），需上游标定确认。
3. **俯仰不可观测**：当前轨迹落在退化运动区间，纯多视几何无法约束俯仰。
4. **数据利用率仅 11%**：缺非关键帧位姿。
5. GNC 参数（μ/c/尺度）对个别外点比例敏感。

### 后续方向（承接 `BA算法自身改进.md`）

1. **合并 V4+V5** 为统一版本（相对球坐标 + 欠约束分类 + 解析稀疏 + GNC）。
2. **改进 5**：视场箱式硬约束（把 elev 限在物理孔径，`trf`+bounds）。
3. **创新点二**：声学阴影→仰角先验，以带不确定度的软观测注入 BA——从"限制俯仰乱跑"走向"真正测量俯仰"。
4. **接入上游真实字段**：landmark_id、标定、协方差、非关键帧位姿。
5. **补真值方案**：仿真高度真值或受控靶，给出 Z 向绝对精度定量结论。

---

## 九、产出物索引

**代码**：`ba_optimize.py`、`ba_improve.py`、`ba_improve34.py`、`ba_patent.py`、`12.5新.py`、`view3d.py`；归档于 `BA代码/`。

**文档**：
- `BA环节总结与上游对接.md`（环节说明）
- `BA代码/上游对接清单.md`（对接规范）
- `BA算法自身改进.md`（9 项改进方向）
- `BA自身改进_测试结果.md`（改进 1&2 数据）
- `BA自身改进34_测试结果.md`（改进 3&4 数据）
- `BA改进实验日志.md`（完整实验日志与机理分析）
- 本文件（阶段工作总结）

**图**：`ba_result.png`、`ba_topview.png`（基线）；`ba_improve_cloud.png`、`ba_improve_zdrift.png`、`ba_improve_metrics.png`（改进 1&2）；`ba_improve34_gnc.png`（改进 3&4）。

**远程**：https://github.com/hardmore123/sfm （最新提交 `72389f0`）。
